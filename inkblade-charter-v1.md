# Inkblade — S1 Charter v1
## The Combo-Orthography Track

**Status: INDEPENDENT TRACK.** This game shares process conventions with the Game Design Lab but is a separate project from Hanzi Sandbox (R4/v3). No fight protocol exists between S1 and any other track — they teach different literacy operations and contest nothing (§6). This document is the single source of truth for S1. Nothing is real until written here.

**This document is self-contained.** An agent with zero prior context must be able to read only this file and (a) understand why the game exists in this form, (b) continue development without drifting, (c) audit any work against the baseline checks in §7.

**Status tokens:** `[LOCKED]` settled · `[LEAN]` working default · `[OPEN]` undecided.
**Roles:** Chat = analysis/arbitration/specs · Design = feel/playtest verdicts · Code = implementation · John = router, binding calls.
**Protocol:** append-only Decision Log (§9), newest first, single-row entries; `[LOCKED]` changes only via superseding entry; decisions as tables, not prose.

---

## 1. The Thesis  `[LOCKED]`

### 1.1 One sentence (the identity claim)

> **The real stroke inventory is the move-set and the real lexicon is the combo book — writing Chinese is literally executing curated combos, and no other game can claim this because no other writing system standardizes production order.**

### 1.2 The anti-pattern: the dictation game

Every stroke-writing product ever shipped (Skritter, tracing apps, school software) shares one structure: writing is transcription to be graded. The player produces strokes; a judge compares to a template; feedback is correct/incorrect. Skill accrues to *pleasing the grader*, not to writing. **A stroke is not input to be judged. A stroke is a verb.**

### 1.3 The substrate

| Orthographic fact | Game-structural equivalent |
|---|---|
| ~8 basic stroke types (横竖撇捺点提折钩) | A fixed move-set of distinct gestures |
| Every character = a standardized stroke sequence | Every character = a combo string |
| Stroke-order rules (top→bottom, left→right, horizontal-before-vertical, outside-before-inside) | Combo *grammar* — patterns that predict unseen combos |
| The lexicon = the sequences history kept | A pre-curated combo book, 3,000 years of curation |
| Each stroke has native direction and shape | Each move has native geometry: the drawn form IS the attack's physical form |

Player skill attaches to: (a) motor execution of stroke gestures — generic; (b) knowledge of specific sequences; (c) internalized order grammar that predicts unseen characters. (b) and (c) **are literacy** — the production side.

### 1.4 The two laws  `[LOCKED]`

**Law 1 — Depth lives in the combo layer.** The slash layer (gesture-combat) is fun themeless (Fruit Ninja is the existence proof) — that satisfies the Illiterate Fun Test but carries zero identity. All mastery growth the game rewards must live in sequence knowledge and order grammar. If playtests show skill growth only in slashing, the model has failed.

**Law 2 — Novelty pressure.** The identity holds only if the winning skill is grammar, not memorization. The content pipeline must keep serving unseen characters faster than players can memorize them. A fixed small roster fails the dark-twin test by construction. (M0 is exempt: its 8-character roster tests the loop, not the pipeline.)

### 1.5 The core loop  `[LOCKED]`

```
A ghost glyph appears (the character to be written; strokes visible as pale targets)
  → each slash IS a physical act; to cut a stroke, the slash's direction
    bucket must match the stroke's canonical direction (Variant B, S1-D003)
  → strokes cut in the canonical sequence ink in GOLD; out of order still cut, ash-grey
  → all strokes cut, order intact → LOCK: the character assembles, speaks its
    pinyin, and its MEANING manifests as the superattack (火 → flame; 木 → tree grows)
  → order broken → FIZZLE: the character crumbles — you still did a thing,
    nothing locked. No "wrong" is ever surfaced. (stability rule, S1-D002)
```

### 1.6 What is rejected  `[LOCKED]`

- ❌ **Variant A — shape-grading** (drawn gesture compared to stroke template form): the dictation game with juice. Direction buckets only; the recognizer must never judge form quality (S1-D003).
- ❌ Correct/incorrect judgments surfaced to the player — lock/fizzle only.
- ❌ XP, streaks-as-reward, badges, timers-as-pressure. (The streak counter is display of fact, not a reward system; the combo book collection IS the content.)
- ❌ In-game tutorial or instruction text. One framing line on the boot screen is the permitted maximum (S1-D007).
- ❌ Stroke-order hints that reveal sequence (the world demonstrates via gold/ash; that is the entire pedagogy).

---

## 2. Why Chinese — uniqueness case  `[LOCKED]`

Alphabets have letter order in *spelling* but no standardized *production* sequence with spatial grammar — no rule system says which pen motion of "W" comes first in a way that transfers across the letterset. Chinese stroke order is (a) standardized, (b) rule-governed by a small learnable grammar, (c) applied uniformly across a lexicon of thousands. It is the only writing system whose production procedure is itself a curated combo system. The concept is not reskinnable to another language — its market identity and its dark-twin test in reverse.

## 3. The three tests  `[LOCKED]`

| Test | Method | Pass condition |
|---|---|---|
| **Illiterate Fun Test** | Tester told nothing about Chinese | Sustained voluntary play; describes it as a slash/combo game, not a language app |
| **Transfer Test** (production-side) | After ~5 hours, show an unseen character | Player attempts a plausible stroke sequence using order grammar (top-first, left-first, outside-first) — not random |
| **Dark-twin test** | Reskin strokes as arbitrary runes with arbitrary sequences | Slash layer survives (expected, fine). Combo layer must collapse: no grammar, no prediction, nothing internalizable. If sequence skill survives the reskin intact, players were memorizing, not reading — audit against Law 2 |

## 4. Nearest relatives and distances

| Relative | Distance |
|---|---|
| **Okami** | Closest ancestor: brush strokes as spells — but ~13 invented whole-shape symbols, no sequence grammar. Here the move-set is a real writing system and the combo book is the lexicon |
| Fruit Ninja | The slash layer alone: no vocabulary, no sequences, no grammar |
| Skritter / tracers | The anti-model: dictation game. No consequence, no world, no verb |
| Fighting games | Sequence execution as mastery — but combos invented per-game; here inherited and meaningful |
| Hanzi Sandbox (R4) | Sibling, not rival: R4 = reading (decompose/compose components, positional grammar); S1 = writing (produce stroke sequences, order grammar) |

## 5. Formal model — v1  `[LEAN]`

### 5.1 Direction buckets (the move-set)

| Bucket | Gesture | Angle center (screen, y down) | Notes |
|---|---|---|---|
| 横 heng | rightward slash | 0° | slight upward tilt tolerated (calligraphically honest) |
| 竖 shu | downward slash | 90° | |
| 撇 pie | down-left sweep | 135° | |
| 捺 na | down-right press | 45° | shares one down-right verb with 点 (S1-D013) |
| 点 dian | down-right flick | 45° | target's own type disambiguates; absolute-length split superseded |

Tolerance ±32° to nearest center; leftward/upward slashes classify to no bucket. **S1-D013:** 捺 and 点 are one direction verb (`dr`); composite compression shrinks 捺 below any fixed length threshold, so length-based discrimination produced false deflects by construction. The target stroke's own type resolves ambiguity; alignment-priority targeting (S1-D008) is unchanged. v2 candidates: 提 折 钩 (`OPEN-2`).

### 5.2 Resolution rules

1. A slash that overlaps no uncut stroke → **whiff** (trail fades, nothing else).
2. **Alignment-priority targeting** `[LOCKED, S1-D008]`: among overlapped uncut strokes, prefer those whose type matches the slash bucket; distance breaks ties. Rationale: a crossing stroke always intersects a slash at distance 0, so min-distance targeting hands a horizontal slash across 十 to the vertical stroke. A slash cuts what it runs *along*, not what it crosses.
3. Bucket matches target type → **cut**. In-sequence while order intact → gold ink; otherwise ash ink, order broken.
4. Bucket mismatches target type → **deflect**: stroke shivers, sparks, metallic ting; stroke stays. (Interesting, not punishing.)
5. All strokes cut: order intact → **lock** (assemble, pinyin speech, meaning effect); else → **fizzle** (crumble, soft gong, small delight).

### 5.3 Character data contract

`{ ch, pinyin, gloss, fx, strokes: [{t: bucket, a: [x,y], b: [x,y]}], comps: [[start,end],…] }` — stroke array order IS canonical global order; geometry in a 0–100 box; `comps` are contiguous stroke-index ranges in canonical component order (global order = component order × internal order, so `comps` adds presentation chunking, not a new rule). Every character's strokes must all live in implemented buckets. **Positional inflection is honored in data:** a component's strokes change form by position per standard orthography (林's left 木 and 炎's top 火 compress 捺→点) — the required gesture physically changes with position. Meaning effect (`fx`) manifests the gloss physically, never as text-first reward.

### 5.4 Composite presentation (S1-D010/D011/D012)

| Element | Rule |
|---|---|
| **Component wash** | Each component's ghost strokes sit on a faint tinted region (diluted ink-wash). Segmentation is perception support — always on; order remains the skill. External sprite/pictorial markers are REJECTED (S1-D010) |
| **Fractal combo / mini-lock** | Complete a component with all-gold strokes → mini-lock (solid ink, pulse, chime); any ash → settles grey. Localizes errors by chunk and by level (internal vs component order) |
| **Breathing hint** | Flail (2 consecutive non-gold) or idle (6s no cut) → the component containing the next canonical uncut stroke breathes; repeat trigger escalates to a stroke comet. Never pre-emptive, never external, composites only |

## 6. Relationship to other Lab tracks  `[LOCKED]`

| | Hanzi Sandbox (R4) | Inkblade (S1) |
|---|---|---|
| Literacy operation | Reading: decode, analyze, compose | Writing: produce stroke sequences |
| Grammar taught | Component position | Stroke order |
| 形声 doctrine | ~80% of lexicon excluded (phonetic misleads meaning-reading) | **No exclusion**: order grammar applies to every character; meaning is granted on completion, not decoded from form. Vocabulary effectively unbounded — exactly what Law 2 demands (S1-D005) |
| Shared inheritance | Stability rule (lock/fizzle, never "wrong"), Fun Gate, ≤3s action-to-response, no engagement-substitutes, verification via real dispatched pointer events, BUILD_ID fingerprinting |

PvP: parked to M2+ (S1-D006). Noted risk: literacy asymmetry becomes directly competitive (more characters known = stronger arsenal) — purest "knowledge is power" and a matchmaking problem simultaneously.

## 7. Baseline Check Protocol — drift detector  `[LOCKED]`

Run on every addition; any wrong answer → stop, escalate to router with a log draft.

| # | Question | Required |
|---|---|---|
| 1 | Does this compare a drawn gesture to a template *form* (curvature, proportion, neatness)? | NO — direction bucket + overlap only |
| 2 | Does this surface a correct/incorrect judgment? | NO — lock/fizzle only |
| 3 | Does this add depth to the slash layer that the combo layer doesn't share? | NO (Law 1) |
| 4 | Does this reward memorizing a fixed roster over inferring order grammar? | NO (Law 2) |
| 5 | Is every new character's stroke data canonical (real sequence, real direction types)? | YES |
| 6 | Does a lock's payoff manifest the character's *meaning* physically? | YES |
| 7 | Slash-to-visible-response ≤ 3 seconds? | YES |
| 8 | Would this mechanic be worth keeping if it taught nothing? (Fun Gate) | YES |
| 9 | Does this add tutorial text, order hints, or XP/badge/streak rewards? | NO |
| 10 | Is the change verified with real dispatched pointer events against a fingerprinted build? | YES |

**Red-flag phrases:** "grade the stroke" · "accuracy score" · "trace the character" · "show the correct order" · "practice mode" · "the player wrote it wrong" · "neatness bonus" · "daily streak reward".

## 8. M0 — the smallest honest prototype  `[LOCKED spec / build shipped]`

**The one question:** is slash-sequencing a visible character by inferred stroke order — and being paid in its meaning — fun for 10 minutes?

| Spec item | Value |
|---|---|
| Build | Single HTML file, canvas, touch + mouse, `inkblade-m0.html` |
| Roster | 一 二 三 十 人 大 木 火 (2–5 strokes, buckets 横竖撇捺点 only) |
| Presentation | One ghost glyph at a time; strokes as pale targets; no enemies, no waves, no health |
| Feedback | Gold/ash ink per cut, deflect on wrong direction, lock (pinyin + meaning effect + cinnabar seal into the combo-book strip) vs fizzle (crumble) |
| Discoverability | Gesture hint on the first glyph (一) only — teaches the slash, cannot leak order (single-stroke). One framing line on boot. Zero in-game text after |
| Instrumentation | `window.__M0_METRICS` (glyphs, locks, fizzles, cuts, goldCuts, deflects, whiffs, perChar) + BUILD_ID in header, on-screen, and `window.BUILD_ID` |

**Go / No-Go (observable):**
- **GO**: tester voluntarily plays >10 min; attempts unseen characters with visible order reasoning (hesitation, deliberate first-stroke choice — grammar engaging, not spamming); ≥1 lock produces visible delight; wrong-order fizzles read as "did a thing," not failure.
- **NO-GO**: testers wait to be shown orders, or stop caring about order once slashing feels good (combo layer not load-bearing → Fruit Ninja with subtitles), or engagement dies <5 min.
- **BOUNCE**: newly discovered defect blocking the metrics — fix and re-run, no verdict filed.

### M0.5 — composites (`inkblade-m05.html`)

**The one question:** do composite characters read as *chunks to conquer in order* rather than stroke soup — without external markers?

| Spec item | Value |
|---|---|
| Composite roster | 休(6) 众(6) 林(8) 炎(8) 森(12) 焱(12) — all bucket-legal, composed of taught primitives 人木火 |
| Curriculum intro | 一 人 木 火 → 休 众 林 炎 森 焱; remaining singles join the random pool after |
| New systems | component wash · mini-locks · breathing hint (§5.4) · merged `dr` verb (S1-D013) |
| Instrumentation | adds `comps{gold,ash}`, `hints{breathe,comet}`, `orderBreaks{internal,component}` — the last distinguishes stroke-grammar errors from component-grammar errors |
| Dev affordance | `?char=X` forces a single-character loop for playtesting |
| 墨 status | excluded — 黑 contains fold strokes (㇕), blocked on OPEN-2, not on this system |

**Go / No-Go (observable):**
- **GO**: tester completes ≥1 twelve-stroke composite; `orderBreaks.component` *decreases* across composite encounters (component grammar transferring); hints stop firing on later composites; time-to-first-cut on a new composite doesn't grow with stroke count (no freeze).
- **NO-GO**: stroke-soup slashing that ignores component structure; hint dependence grows instead of shrinking; 12-stroke ghosts cause quit or visible dread.

### M0.75 — the living scroll (`inkblade-m075.html`)

**The one question:** does an accumulating, persistent, *alive* world make locks feel like building — observed via voluntary re-locks (repeating a known character for the world's sake) and unprompted attention to the painting? (No session-length metric per router ruling.)

| Spec item | Value |
|---|---|
| Persistence | Every lock plants its meaning into the scroll; fizzles leave nothing (stability rule extended, S1-D016); world survives reload via localStorage; `?reset=1` clears |
| Life | Everything idles forever: walkers amble and turn, trees sway and shed leaves, fires flicker and spit embers, mist drifts across peaks, the resting figure breathes |
| Duplicates | Encouraged — each instance is a per-seed individual (size, gait, lean, foliage). First-ever lock plants with a gold ripple flourish; repeats plant quietly |
| Leverage | 森 plants three trees in one lock, 众 a walking crowd of three, 一 draws the horizon (unique, replaced not duplicated), 大 raises a peak, 十 lays a path crossing |
| Composition | Auto-composed into shan-shui bands (sky/far/mid/near); no player placement (a bad garden would kill the delight this build tests — placement belongs to the Level-3 choice layer) |
| Sky axis | 日/月 (day/night) gated on OPEN-2 — fold strokes; the world gains a sky when the move-set does |
| Instrumentation | adds `world.elements`, `world.relocks` (the motivation metric) |

## 9. Open Questions Registry  `[OPEN]`

| id | Question | Blocking? |
|---|---|---|
| OPEN-1 | ~~dian/na length threshold~~ **CLOSED by S1-D013** — merged down-right verb; target disambiguates | — |
| OPEN-2 | ~~提/折/钩 buckets~~ **DESIGN CHOSEN by S1-D021** — polyline verb model (corner-split trail, ≤3 direction-class segments, anti-grading invariants); implementation = handoff C1 | M1b |
| OPEN-3 | What gives slashes purpose *between* locks — **path chosen:** demand world (OPEN-9) is the diegetic answer; ecology E1 is its physics substrate | M2 |
| OPEN-4 | ~~Content pipeline~~ **RESOLVED by S1-D021** — offline character-pack pipeline from stroke DB; implementation = handoff C2 | M1a |
| OPEN-5 | Same-type stroke disambiguation (三's hengs; 焱's six pies/dians): alignment-priority + dense-glyph tolerance handles scripted play; does it survive fast sloppy human play? | human verdict |
| OPEN-6 | PvP model (literacy asymmetry as arsenal) | M2+ |
| OPEN-7 | ~~众/焱 compressed forms~~ **RETIRED by S1-D021** — pipeline derives canonical forms from real glyph data | — |
| OPEN-8 | Component wash: always-on, or fade as a character family is mastered? (Mastery-fading is elegant but adds a progression system — Fun Gate check required) | M1 |
| OPEN-9 | **Demand world** (S1-D016 Level 3, M2 headline): world state generates wants ("the grove is dark — write something that burns"), repertoire *choice* replaces some ghost glyphs. Turns production from copying into composing; solves OPEN-3 diegetically; needs a want-authoring model that never becomes a quest log. Chat design task | M2 |
| OPEN-10 | ~~Ecology import ruling~~ **RESOLVED by S1-D020** — two-phase adoption (E1 now, E2 hard-gated); router to add one-line kinship cross-reference in the R4 charter | — |
| OPEN-11 | ~~Hook matching~~ **CLOSED by S1-D034** — optional: a straight shu slash cuts a 竖钩; the hook is polish, never required, never punished | — |
| OPEN-12 | ~~Stroke-DB licensing~~ **RESOLVED by S1-D035** — no independent PRC-standard DB exists; MMAH's 1999 APL is free copyleft (commercial use permitted); packs ship as APL-licensed data artifacts, license vendored | — |
| OPEN-13 | ~~Depth-1 component chunking~~ **CONFIRMED by S1-D037** — chunk = leaves of the char's own decomposition tree, non-spatial IDCs collapse, sub-2-stroke chunks merge | — |
| OPEN-14 | ~~E2 choice axis~~ **RESOLVED by S1-D049, AMENDED by S1-D059** — aim-based placement choice (M2a) superseded: John's playtest ruled release-point aiming awkward, chance placement + collision-triggered reactions preferred; want-driven repertoire stays M2b | — |
| OPEN-15 | ~~Flat-撇 (平撇) gap~~ **RESOLVED by S1-D049** — flat-撇 accepted as a real form of 撇 at derive AND match (amends S1-D032); implementation = M1f-b2 per S1-D050 | — |

## 10. Decision Log — append-only, newest on top

| date | id | decision | by | status | supersedes |
|---|---|---|---|---|---|
| 2026-07-11 | S1-D070 | **M2e-b1 shipped and verified** (`inkblade-m2e.html`, BUILD_ID **S1-M2e-b1-20260711**; m2d freezes; helpers TARGET bumped) — **the ink travels**, per S1-D069 exactly: placement chooses at lock (`spawnWorldFor` returns transit-flagged els; render, contact shadows, and the whole sim skip them — a flying fire gives no heat and its life clock is frozen until landing; `saveWorld`'s explicit field pick omits the flag, so a mid-flight reload completes the plant instantly); coalesce ≈300ms (the glyph's strokes contract to their centroid AS the gold flash — originals hide, the resolve-lock overlay skips while a transit runs); flight ≈620ms (quadratic arc with TRANSIT_ARC_LIFT, droplet shrinks toward the target's `depthK` — departure INTO the depth — gold→ink trail; group members stagger 90ms); arrival stamps `born` (the existing 700ms grow-in + fresh gold ring play unchanged), splashes ink particles, sounds a soft arrival tone, and **the banner now rises at the arrival, anchored at the element's converged x — the name appears where the thing appears**. reduce-motion hosts reveal instantly (the pre-M2e look). Total plant latency ≈920ms ≈ the old +900ms setTimeout, so world timing is unchanged by design and no suite wait needed retiming. Verified smoke20 (7 tests): transit contract (chosen at lock, revealed on arrival); `?seed=` determinism byte-identical across runs (the worldRand stream is untouched); grove stagger measured 83ms; banner anchored at `worldScreenX` to 1e-5; a flying fire's `life.t` frozen at 0 then ticking after landing; mid-flight reload lands the element with no transit; 115 dark px of real ink at the anchor post-arrival. **Full battery green on this build: smoke5–20 + frozen smoke1–4, zero console errors; smoke12 perf 16.56ms avg @300 (absolute budget — better than m2c-b2's 17.41ms; the transit costs nothing measurable).** Eyeball: the mid-flight droplet arcs with its trail while the world holds; the tree blooms where the drop lands with "mù / tree" named beside it. Next: STOP at the M2e-b1 checkpoint (§11) — verdict wanted on transition legibility/feel; the motion-parallax camera (S1-D068(2) step 2) may plan in parallel | code (Claude) | LEAN | — |
| 2026-07-11 | S1-D069 | **M2e-b1 plan — the ink travels (lock transition per S1-D068(2); exploration doc §2).** The gap John named: on lock the glyph flashes center-screen and 900ms later its element pops in at a random spot — nothing connects the character to its object ("not always obvious which character is projected to what object"). The fix is Crayon-Physics ink continuity, all Canvas 2D: (1) **Placement chooses at lock time** — `spawnWorldFor` plants immediately but flags elements `transit: true`: skipped by `drawWorld`/`drawContactShadows`, excluded from `updateWorld` sim (no heat sourcing, no hot-stamp, no life/burn/agent ticking — clocks start at arrival), and never persisted (`saveWorld`'s explicit field pick already omits the flag → a mid-flight reload completes the plant instantly, fire's Q12 pattern). `worldRand` call order untouched → `?seed=` determinism holds byte-for-byte. (2) **Coalesce (~300ms)**: the lock's gold flash becomes the glyph's own strokes contracting to their centroid (normalized snapshot of the real polylines; originals hide; the resolve-lock gold overlay skips while a transit runs). (3) **Flight (~620ms)**: an ink droplet arcs (quadratic bezier, TRANSIT_ARC_LIFT×H) from the centroid to each element's converged anchor (`worldScreenX`), shrinking toward the target's `depthK` — the departure into depth is itself a depth cue; tapered gold→ink trail. Group members stagger ~90ms (林 = a small squadron of droplets). (4) **Arrival**: transit clears, `born` stamps now → the existing 700ms grow-in + fresh gold ring play unchanged; ink-splash world particles + a soft arrival tone. (5) **The banner moves to arrival and to the object** — pinyin/gloss appear beside the planted element (normalized anchor, clamped on-screen above the ledger), not center-screen at lock: the name appears where the thing appears. (6) `reduce-motion` hosts skip the animation (instant reveal, old look). (7) Constants in 00-config (TRANSIT_COALESCE_MS 300 / TRANSIT_FLIGHT_MS 620 / TRANSIT_STAGGER_MS 90 / TRANSIT_ARC_LIFT); `state.transit` single-slot with defensive force-complete. (8) New artifact `inkblade-m2e.html` (m2d freezes), BUILD_ID S1-M2e-b1-20260711, helpers TARGET bumps. (9) **smoke20-transit.js**: transit flagged at lock and cleared after flight with `born` stamped at arrival; `?seed=` determinism across two runs; group arrivals stagger-ordered; banner absent mid-flight / present near the element after; fire's life clock frozen during flight and ticking after arrival; mid-flight reload lands the element (count kept, no transit); post-arrival ink present at the anchor. Timing audit: elements now EXIST from t≈0 (earlier than the old +900ms) and reach full size on the same schedule (~1.6s) — count/pixel waits in smoke5/10/13/14/17/19 unaffected; full battery gates the ship | code (Claude) | LEAN | — |
| 2026-07-11 | S1-D068 | **Checkpoint-3 verdict (John) — M2d-b1 ACCEPTED; visuals are cosmetic; the sandbox interaction web is the next MAJOR milestone.** Verbatim substance: "m2d-b1 is done"; "the core gameplay loop is pretty much fixed. 3d and visuals are purely cosmetic"; "batch1 symbols are acceptable but we can improve later when the swappables are ready"; "the only true enhancement on game loop itself is the interactions between objects which relate back to sandbox world idea. it is the next major milestone. we can go ahead with your suggestion, but keep note of the future goal". Context: John's three asks of 2026-07-11 (3D obviousness / character→object transition / visual-assignment modularity) were answered by the exploration doc `inkblade-3d-transition-visuals-exploration.md` (committed 42e84e8). Four rulings: (1) **M2d-b1 quality gate PASSED** — but chunk C's scaling strategy CHANGES: no further bespoke-batch grinding now; the remaining 277 basic-tier seals get faces when the swappable sprite pack block exists (doc §3 — visuals become pack data, updatable without engine rebuilds), and batch-1 symbols improve then too. (2) **Cosmetic track approved in the doc's order**: lock transition (M2e) → motion-parallax camera → sprite block + coverage → WebGL billboard pilot ONLY if the parallax checkpoint still reads flat (each step: plan entry + checkpoint per S1-D058). (3) **Object↔object interactions — the sandbox world web — is the next MAJOR milestone** after the cosmetic chunk; it reframes M2b's headline (demand-world wants may ride on it, OPEN-9 stays open); groundwork discipline: every new kind keeps shipping with tags (the S1-D066 pattern) so the interaction web has matter to react to. (4) **Standing note on every cosmetic plan**: keep the future goal visible — cosmetic work must not grow mechanics debt that blocks the interaction milestone (concretely: transit matter is excluded from sim so interaction rules never fire on unlanded ink; sprite kinds stay promotable to living renderers with the kind key unchanged) | John | LOCKED | S1-D059(3) (chunk C scaling path), OPEN-9 (reframed) |
| 2026-07-11 | S1-D067 | **M2d-b1 shipped and verified** (`inkblade-m2d.html`, BUILD_ID **S1-M2d-b1-20260710**; m2c freezes; helpers TARGET bumped) — chunk C batch 1 per S1-D066, plus one pipeline addition the plan implied but didn't name: **`overrides.charsExtra`** — curation for chars OUTSIDE the core roster (the first cut accidentally grew core 23→32 because the core roster IS `Object.keys(overrides.chars)`; charsExtra keeps core at 23 while basic-tier chars carry hand-picked worlds). Shipped: 7 bespoke kinds (大 bigfig — the pictograph itself, a big figure with arms spread, living and ambling; 门 gate, 马 horse (living), 心 pulsing heart, 风 rolling wind swirls, 电 cloudlet+flickering bolt, 车 cart) + 3 tier-2 families (banner flag/scroll ← radicals 口讠言纟; dwelling hut/pavilion ← 宀, 家 curated hut; skylight glow/dawn ← 日, 天 curated glow) + flora `flower` form (花). **Basic-tier seals: 377 → 277** (100 chars gained faces this batch: banner 53, figure +扌辶, dwelling 8, skylight 7, bespoke 9, flora 1). Interaction-readiness = tags in pack data: horse/bigfig `living` (E1 startle/warm/rest applies from day one), dwelling `shelter+flammable`, banner `flammable`. smoke19 proves it live: a walker RESTS beside a hut (the R3 rule, no new code), and a hut kindles from adjacent fire then regrows into ITSELF (seed+form kept, destructions 0) — John's "person watering trees" future has its grammar. Config rows only in engine (bands/ZI/DEPTH_EXEMPT+skylight); renderers are brush-style, kid-first. Eyeball: the gallery reads as a picture book — figure, horse, gate, hut, pavilion, heart, flower, cart, banners, sky glows. Full battery green: smoke5–19 + frozen smoke1–4, zero console errors. Next: STOP at the M2d-b1 checkpoint — John's quality verdict gates SCALING the symbol work (277 seals remain; next radical families are ready to name: 亠冖八力刀又土王白田目...) | code (Claude) | LEAN | — |
| 2026-07-10 | S1-D066 | **M2d-b1 plan — chunk C first batch: every character earns a face (S1-D059(3), S1-D063 children's-eyes law; John 2026-07-10: symbols make the game fun to look at AND prepare future interactions, e.g. person watering trees).** Current gap measured: 377/500 basic chars are plain seals; 大/山 share `peak`. First batch = prove the quality bar, then STOP for John's verdict before scaling (his explicit rule). Two tracks: **(1) Bespoke tier-1 kinds** (7 new brush renderers, each kid-readable at a glance): 大→`bigfig` (a BIG figure, arms spread wide — the pictograph itself; living, wanders, dedupes from 山), 门→`gate` (open doorframe), 马→`horse` (brush horse; living — E1 startle/warm for free via tags), 心→`heart` (soft cinnabar heart, gentle pulse), 风→`wind` (animated swirl strokes), 电→`bolt` (cloudlet + zigzag flicker), 车→`cart` (two wheels + body). **(2) Tier-2 family renderers** (3 new classes + 1 form, converting seal blocks at scale via radicalClasses): `banner` (fluttering word-flag/scroll; radicals 口/讠/言/纟 — speech and thread chars become little banners; flammable — paper burns), `dwelling` (hut/pavilion; radical 宀; 家 curated to dwelling:hut; shelter+flammable — walkers will REST by houses via the existing R3 tag rule, and E2 can kindle them with regrowth honoring the covenant), `skylight` (glow/dawn in the sky band; radical 日; 天 curated to skylight:glow), flora gains a `flower` form (花 curated; radical 艹 keeps its forms). Also new radical mappings reusing EXISTING renderers: 扌→figure(stoop — working hands are a working figure), 辶→figure(stroll — walking chars walk). **Interaction-readiness is tags, not code**: living/shelter/flammable in the kinds block plug the new matter into E1/E2 today and future rules (watering, wind-fans-flame, lightning-strikes) later. Engine changes: renderers + bands/ZI presentation rows only (content stays pack data); skylight joins DEPTH_EXEMPT (sky). New artifact `inkblade-m2d.html` (m2c freezes), helpers TARGET bumps, BUILD_ID S1-M2d-b1. **smoke19-symbols.js**: pack contract (batch chars carry the designed kinds/classes in both packs, 大≠山 at last, tags as designed, seal count in basic ≤300 — pins the conversion), end-to-end locks for a bespoke sample with real gestures (right kind plants, visible ink at the anchor), 大/山 distinctness, and a fast-clock interaction scene (walker rests by a dwelling; the dwelling kindles from adjacent fire and regrows into ITSELF; destructions 0). Full battery gates the ship; checkpoint follows immediately with a what-to-write list for John | code (Claude) | LEAN | — |
| 2026-07-10 | S1-D065 | **M2c-b2 shipped and verified** (`inkblade-m2c.html`, BUILD_ID **S1-M2c-b2-20260710**) — the illustrated valley per S1-D064, exactly as planned: `drawBackdrop()` opens `drawWorld` (empty-world early-return removed — an empty world is still a place), 3 overlapping ridgeline silhouettes (alpha 0.20/0.145/0.09, farther = paler) filling down to the GROUND_FAR line, 4 slow-drifting clouds (paper-plus-white puffs, ~2–3 min per crossing — watchable), 56 ground tufts/pebbles with perspective density and depth scaling, all laid out once from FIXED seed 42 (the valley is the same place every session; `?seed=` world randomness untouched, geometry normalized so resize needs no rebuild). Backdrop rides the world layer → recedes behind the veil while writing, blooms on lock. `__S1_SCENE` exposes counts. smoke18 T6 added: empty world shows the backdrop (3/4/56) AND the ridge band actually tints pixels (1,200 tinted px probed) while T5 re-proves zero ink under both suites' thresholds in all four zones — silk, not ink. Perf: smoke12 T3 passed the ABSOLUTE budget with the backdrop included (17.41ms avg at 300 elements — the backdrop is near-free; cached geometry, ~63 path fills/frame). Full battery green: smoke5–18 + frozen smoke1–4, zero console errors. Eyeball: the title screen alone now reads as a valley — hills in the middle distance, clouds overhead, tufted ground. Next: STOP at the M2c-b2 checkpoint (§11) — the hollow verdict (S1-D063) is what this build answers, so John's re-read is the gate; chunk C plan may draft in parallel | code (Claude) | LEAN | — |
| 2026-07-10 | S1-D064 | **M2c-b2 plan — chunk B2: the illustrated valley (per S1-D063).** The hollow read comes from the world being an empty paper void: nothing exists except what the player has planted. Fix: an always-present illustrated backdrop, engine-side presentation furniture (like the veil or the mist — no pack content, no save impact, deterministic from a FIXED seed so the valley is the same place every session, ?seed= untouched). (1) `drawBackdrop()` opens `drawWorld` on the world layer — it recedes with worldAlpha while writing and blooms on lock, so the backdrop joins the attention choreography instead of competing with it; the empty-world early-return goes (an empty world must still be a place). (2) Contents, far→near: 2–3 overlapping distant ridgeline silhouettes above the horizon band (soft blue-gray, alpha ≤0.20 — visible but far above every ink-probe threshold); 3–4 slow-drifting clouds (paper-plus-white — brighter than paper, can never read as ink); scattered ground tufts/pebbles with perspective density (bigger+denser near, alpha ≤0.15), positions cached once per resize. (3) `window.__S1_SCENE = {ridges, clouds, tufts}` for tests. (4) smoke18 gains T6: empty world still shows the backdrop (SCENE counts > 0) AND stays zero under both ink thresholds (T5 re-asserts on the same build — silk, not ink). (5) Child-clarity guardrail: silhouettes must read as hills/clouds at a glance, nothing abstract. (6) BUILD_ID S1-M2c-b2, same artifact, full battery incl. the smoke12 min-of-2 perf gate (backdrop must stay ~free: cached geometry, no per-frame allocation beyond 2 existing mist gradients) | code (Claude) | LEAN | — |
| 2026-07-10 | S1-D063 | **Checkpoint-2 verdict (John) — depth works, world is hollow; AUDIENCE IS CHILDREN.** Verbatim substance: he can see depth ("not all character creatures are at the same ground level"), but "lack of background and terrain makes it look pretty hollow, so the 3D effect is not so obvious"; asked whether "phase.js" (Phaser) has native 3D we could use; and — **first explicit statement of audience — "remember this is supposed to be game for children."** Three rulings: (1) **Children are the audience** — a standing design law from here on: visual language must read at a glance (picture-book clarity), interactions stay wonder-forward (the transformed-≠-destroyed covenant already fits), no dark/punitive feedback. (2) **Investigated the engine question honestly**: Inkblade uses NO framework (vanilla Canvas 2D, single-file, dependency-free per the build discipline — John misremembered); web search confirms Phaser 3 AND Phaser 4 (Apr 2026) are natively 2D-only (Phaser 4 Mesh2D carries no depth; pseudo-3D is plugins), and the ecosystem's real-3D route (enable3d) is a three.js wrapper — i.e., any "3D Phaser" is a full three.js rewrite. Ruling: **no engine switch** — a rewrite would invalidate the entire suite battery and the single-file discipline to solve a problem that is content-shaped, not engine-shaped. (3) **The hollowness is the actual gap**: the world has no background or terrain — nothing exists except planted elements on blank paper. Fix in-engine: an illustrated backdrop layer (distant ridgelines, drifting clouds, ground tufts — the picture-book valley), chunk B2, immediately | John + code (Claude) | LOCKED | — |
| 2026-07-10 | S1-D062 | **M2c-b1 shipped and verified** (`inkblade-m2c.html`, BUILD_ID **S1-M2c-b1-20260710**; m2a freezes; helpers TARGET default bumped) — chunk B per S1-D061, with two implementation deviations logged honestly: (1) per-element atmospheric fade became a **second mist stage** (0.72→0.86, alpha 0.09→0) — canvas `globalAlpha` doesn't compose with renderers that set their own alphas, so the fade lives in the compositor where it also covers particles; (2) contact shadows are **one batched path filled at a single alpha** — the naive per-element save/alpha/fill version cost ~5ms/frame at 300 elements (caught by smoke12's perf gate before ship) and single-alpha batching also stops overlapping shadows double-darkening. Shipped as planned: perspective convergence (PERSP_FAR 0.80, render-only — smoke18 T2 proves persisted x stays world-space after a real lock), depth scale 0.42/1.45 (3.45× contrast), sky/ground washes + foreshortened grain on the paper texture (probed: ZERO ink under both suites' pixel thresholds in all four zones), windowed roster ledger (44px tiles at 509 chars AND at 23, gold-ringed current tile, edge fade, locked/total·packId counter, paper backdrop band so near-field walkers don't clutter it, `__S1_HUD` for tests). **smoke12 T3's throttled-host calibration hardened** (extend pattern): single samples on a throttled host swing ±3ms and flapped the gate at a true delta of ~+1ms — each build now samples twice and compares MINs (noise only adds time; a real regression still shows in the min; the 15% margin and the capable-host absolute budget stand). Measured honestly: min-of-2 target 23.66ms vs m1d baseline 22.80ms (+3.8%) on this powersave host — m2a measures identically (~25ms single-sample), so chunk B's true frame cost is ~1ms. Eyeball: near trees tower over far ones at 3.45×, the ground field converges, shadows anchor matter, the fire glows mid-field, the ledger reads at a glance. Full battery green: smoke5–18 + frozen smoke1–4, zero console errors. Next: STOP at the M2c-b1 playtest checkpoint (§11), then chunk C (visual symbols, S1-D059(3)) | code (Claude) | LEAN | — |
| 2026-07-10 | S1-D061 | **M2c-b1 plan — chunk B: the scroll gains real depth and a legible ledger (S1-D059(2)(4); answers the standing S1-D040 3D ask).** Diagnosis first: the world doesn't read as 3D because M1d's depth staging is scale+mist only — no ground plane, no perspective convergence, weak scale contrast (2.3×), nothing anchoring matter to the ground; and the roster HUD lays ALL chars in one row (`cell = (W-32)/n - 6` → ~2px cells at n=500 — the exact "too small to see" John hit). The build, all presentation-layer (sim coords, saves, bands, packs, E1/E2, veil untouched — Q12 trivially holds): (1) **Perspective convergence, render-only**: ground elements' screen x converges toward the vanishing center with distance — `worldScreenX(el)` maps `0.5 + (el.x−0.5)·(PERSP_FAR + q·(1−PERSP_FAR))`, PERSP_FAR 0.80, q = depth position; sky/horizon exempt; applied as a translate in `drawWorld`'s per-element transform so every renderer inherits it; `el.x` itself never changes (saves + E1/E2 distances stay world-space). (2) **Stronger depth scale**: DEPTH_SCALE 0.55/1.25 → 0.42/1.45 (far-to-near ratio 2.3×→3.5×). (3) **Atmospheric per-element fade**: alpha multiplier 0.78 far → 1.0 near on top of the mist band. (4) **Contact shadows**: a soft ground ellipse under every ground element, depth-scaled, alpha ≤0.12 — anchors matter to the plane (the single strongest "standing ON ground" cue). (5) **Scene furniture on the paper texture** (static, behind the world): subtle sky wash above GROUND_FAR, warm ground wash below deepening toward the near edge, and foreshortened paper grain (grain strokes compress toward the far line) — all tones ≥ r200 so no existing ink probe (r<110 smoke12, r<150 smoke13) can ever count them. (6) **Roster HUD redesign**: a windowed strip of ~11 legible cells (min(44, W/13)px) centered on the current glyph's roster index — locked = cinnabar tiles, current = enlarged gold-ringed tile, upcoming = outline+dot, edge fades when the roster continues — plus a `locked/total` counter and pack id; identical at 23 and 500 chars; `window.__S1_HUD` exposes {cellPx, lockedCount, total} for tests. (7) New artifact `inkblade-m2c.html` (m2a freezes); helpers TARGET default bumps. (8) **smoke18-scene.js**: projection contract (far converges toward center, near ≈ unchanged, horizon/sun exempt), roster legibility at the 500-pack (cellPx ≥ 28 via __S1_HUD), furniture-is-not-ink (empty-world probe counts 0 under both thresholds); full battery gates the ship. Verified-risk notes: smoke12 T2's far probe tolerates the ≤30px convergence shift (90px half-box); world particles keep unconverged coords (≤30px drift at far, accepted; near ≤15px) | code (Claude) | LEAN | — |
| 2026-07-07 | S1-D060 | **Chunk A shipped and verified** (`inkblade-m2a.html`, BUILD_ID **S1-M2a-b4-20260707**) — the aim mechanic retired per S1-D059(1). `placeEl` reverted to pure max-spacing candidate scoring (no lockExit, no pull weight); `state.lockExit` removed entirely (01-state.js, 06-combat.js); `ecology.placement` dropped from `pipeline/overrides.json` and both rebuilt packs. Ignition/regrowth mechanics (M2a-b2) are UNCHANGED — heat-proximity ignition already was the "collision → interaction" model John asked for; only the aim bias on top of it is gone, so a planted fire now only catches something if the dice actually put it nearby. smoke16 inverted (gate-flip pattern): T1 confirms neither pack carries `placement.pull`; T2/T3 prove the same seed plants IDENTICALLY regardless of the final stroke's exit point (exit-independent, to machine precision); T4 confirms different seeds still land differently (genuinely random, not frozen). smoke17 T7 rewritten: no more flick-toward-the-tree — a tree is seeded at the exact spot seed 24's random draw is known to land a fire (found empirically), so the fire lands on it by accident and kindles it, proving the "creative collision" story end-to-end without any aiming. Full battery green: smoke5–17 + frozen smoke1–4, zero console errors. Chunk B (canvas/UI redesign + 3D legibility) and chunk C (visual symbol expansion, first batch) are next, each its own plan entry and checkpoint per S1-D058 | code (Claude) | LEAN | S1-D052, S1-D053 |
| 2026-07-07 | S1-D059 | **Checkpoint 1 verdict (John) — four rulings, M2b postponed.** John played `inkblade-m2a.html`. Verdict, verbatim substance: (1) **Placement-choice aiming feels awkward — release point is very low priority.** He wants pure chance: things land randomly, and *if two objects happen to collide, the interaction happens* (fire+wood=burn, fire+person=drive away) — a physics-box-check model, not a targeting act. Ruling: **S1-D052/D053 (the aim half of M2a) is SUPERSEDED — `placeEl` drops the lockExit pull entirely, back to pure max-spacing random placement.** The reactive tag-rules already in the engine (E1 startle-from-heat, E2 ignite-from-heat) already ARE the "collision → interaction" model he's describing — proximity, not aim, decides outcomes; removing the aim bias makes that legible instead of feeling authored. Expanding the *variety* of interaction pairs is content work, folded into the next chunk alongside new kinds (a new kind is what a new interaction needs to react to). (2) **The 3D-reading world (his standing S1-D040 ask) still isn't landing.** Investigated: no true 3D was ever built or promised past M1d's depth-staging (`depthK` y-as-depth scale/mist, S1-D041/S1-D042) — a 2D canvas illusion, not a 3D scene. It shipped and measured, but evidently doesn't read as "3D" to him at the table. Folded into the canvas-redesign chunk (item 4) rather than treated as a separate ask. (3) **Visual symbol coverage is inadequate — wants every character visually unique.** Investigated and confirmed concretely: only 13 `world.kind` families exist total; **489 of 500 basic-tier characters (98%) carry no kind at all and fall back to the plain seal** (character glyph in a stamp, no scenery); of the ones that DO have a kind, 大 (big) and 山 (mountain) literally share one kind (`peak`, differ only by scale) — the exact duplicate he flagged; 二/三/十/大 (numbers) map to abstract terrain kinds (ridge/terrace/path/peak) that don't read as their meaning. Good examples he named (person/fire/wood/forest/mountain/sun/moon) are exactly the hand-curated kinds with dedicated renderers — the fix is more of that, not a different technique. Ruling: Code attempts this itself (procedural canvas iconography, radical-driven per the existing `radicalClasses` backlog item, leaning on the fact most of these characters are literally pictographic) rather than outsourcing to another AI; ships a first batch and checkpoints on quality/legibility BEFORE scaling to the full 500 (then the 3,000–7,000 roster, S1-D043). (4) **Canvas needs a full redesign** — the 500-char roster view is too small to read; this waits until the symbol work has enough new visual material to lay out, and folds in the depth/3D legibility fix from (2). **M2b (demand world, OPEN-9) is POSTPONED — everything else waits on these four.** Priority order Code is executing in: chunk A (this entry) — drop the aim bias, ship, checkpoint; chunk B — canvas/UI redesign (+ 3D legibility); chunk C — visual symbol expansion, first batch, checkpoint before scaling. supersedes S1-D052/D053 (aim half only; ignition/regrowth mechanics from M2a-b2 stand unchanged) | John | LOCKED | S1-D052, S1-D053 |
| 2026-07-07 | S1-D058 | **Playtest-checkpoint mandate (John).** After M2a shipped, John playtested and could not find the promised changes (burning, lifecycle, the 500 chars) — root causes: he likely opened an older frozen artifact and/or `file://`, where `?pack=` chapter loading cannot fetch, and fire clocks run real minutes. Ruling: **since the project is exactly Code + John, every milestone (and each reasonable-size shipped build) must end at a PLAYTEST CHECKPOINT — Code stops and asks John to play, with concrete instructions: exact artifact/URL + server command, query params and state conditions, what's new versus the last snapshot he played and where to look, a short action script with expected timings, and what verdict is needed. Code may continue unblocked work in parallel but must not stack unplaytested player-facing changes.** Checkpoint verdicts get logged; floating "needs a John playtest" questions (OPEN-5) attach to checkpoints. Codified in `CLAUDE.md` (session instructions, checked in) | John | LOCKED | — |
| 2026-07-07 | S1-D057 | **M2a-b3 shipped and verified — E2 IS LIVE; M2a complete** (`inkblade-m2a.html`, BUILD_ID **S1-M2a-b3-20260707**). The flip per S1-D056: `E2_ENABLED` true, `?e2=0` kill switch verified in both suites that read the gate. Gate-polarity assertions inverted per plan (smoke6 sunset pattern): smoke11 T1 asserts default-ON + kill-switch; its E1 scenarios ran byte-identical (fixture carries no ignition data — data-gating held as the fence). smoke17 T1 asserts a bare boot kindles and `?e2=0` keeps the same world inert. **New smoke17 T7, the milestone promise end-to-end with real gestures**: a tree stands in the world, 火 is written and its final stroke flicks through toward it — the fire plants 0.012 from the aim (placement), kindles the tree (E2), and the tree regrows into itself; locks 1, destructions 0 (T7 fixture note: seed 24 — seed 23's candidate pool had nothing within kindle range, an honest miss placement can produce; fixture flame r 0.10 and T5b geometry adjusted so the one-hop negative sits outside the fire's own radius but inside the burning neighbor's). smoke13's hearth scene survived the flip untouched — its tree sits outside the shipped ignition radius, so the E1 hearth stays a hearth. Full battery green: smoke5–17 + frozen smoke1–4, zero console errors. **M2a closes: OPEN-14 resolved and shipped; the S1-D020 gate is discharged — writing is aiming, fire is consent-safe arson, and the world answers. M2b (demand world, OPEN-9) is NEXT** | code (Claude) | LEAN | S1-D020 (E2 gate lifted) |
| 2026-07-07 | S1-D056 | **M2a-b3 plan — the E2 flip.** The S1-D020/S1-D046 gate condition — "default-ON only after playtest confirms the choice axis reads" — is met on the recorded evidence (delegation per S1-D049): smoke16 proves placement follows the gesture, the b1 eyeball shows the campfire landing where the flick ended, smoke17 proves ignition transforms and regrows without ever destroying. (1) `E2_ENABLED` → **true**; `?e2=0` remains the kill switch (`__S1_FLAGS.e2` reports; S1-D054's runtime resolution unchanged). (2) Gate-polarity assertions follow the designed flip, the smoke6 alias-sunset pattern: smoke11 T1 asserts default-ON + kill-switch-OFF (its E1 scenarios stay byte-identical — its fixture pack carries no ignition data, so the flip cannot touch them: data-gating IS the regression surface now); smoke17 T1 inverts to assert the default kindles and `?e2=0` prevents it. (3) New smoke17 T7 — the milestone promise end-to-end with REAL gestures: in a world holding a tree, 火 is written and its final stroke flicks through toward the tree; the fire plants beside it (placement pull), kindles it (E2), and the tree walks to regrowth; destructions 0 throughout (fixture gains the `placement` block; deterministic seed). (4) BUILD_ID S1-M2a-b3, full battery, ship entry closes M2a; M2b (demand world, OPEN-9) becomes NEXT | code (Claude) | LEAN | S1-D020/S1-D046 (gate condition discharged) |
| 2026-07-07 | S1-D055 | **M2a-b2 shipped and verified** (`inkblade-m2a.html`, BUILD_ID **S1-M2a-b2-20260707**) — E2 ignition + regrowth per S1-D054, gate still OFF (default; `?e2=1` playtests it). Exactly as planned: `E2_ON` runtime gate + `__S1_FLAGS.e2` reporting; `updateIgnition` (rate-accumulated dwell, per-stage `ignition.flame/ember {r, dwellMs}` + `coolMs`, all pack data, runs only when the pack carries BOTH `ignition` and `regrow`); `updateBurnLife` walks burning→embers→smoke on fire clocks then ash→sprout→sapling on `regrow` clocks and deletes `el.burn` — kind+seed never change; render splits matter (world layer) from firelight (burn-through overlay), regrowth phases replace the form, the sapling is the base renderer at phase-scaled size — THE SAME tree returns; save v2 gains additive `burn` field (freezes while away, fire's Q12 pattern). Shipped tuning: flame r 0.07 dwell 5s, ember r 0.045 dwell 14s, cool 8s; regrow ash 70s/sprout 40s/sapling 40s (full tree cycle ≈ 4.6 min). Verified smoke17 (fast-clock fixture `eco-ign.json`): gate holds by default; flame ignition transforms (count stable, e2.ignitions counts); full walk burning→embers→smoke→ash→sprout→sapling→itself with e2.regrowths counting; ember-stage kindle proven AT the ember phase with the per-stage radius negative (inside flame-r/outside ember-r never catches); sun adjacency never ignites; one-hop verified through a full burn (no chaining); mid-burn reload resumes and completes; e1.destructions 0 in every scenario. Eyeball: flames wrap the tree while its canopy stands (transformed, not replaced), neighbor untouched, later an ash mound with a green sprout. Full battery green: smoke5–17 + frozen smoke1–4, zero console errors. Next: b3 = the flip | code (Claude) | LEAN | — |
| 2026-07-06 | S1-D054 | **M2a-b2 plan — ignition + regrowth behind the gate (S1-D049c/d; flag still OFF, flip is b3).** The covenant, mechanized: (1) **Runtime gate `E2_ON`** resolves in 03-canvas — `?e2=1` playtest enablement pre-flip, `?e2=0` kill switch post-flip, default = `E2_ENABLED` (unchanged, false); `__S1_FLAGS.e2` reports the resolved value. (2) **Ignition** (`updateIgnition`, runs only under `E2_ON` AND pack `ecology.ignition` present): live heat (`heatSources()` — fire kinds only, so one-hop is by construction and the lifeless sun can never ignite) kindles `flammable` non-burning matter by per-stage pack params — `ignition.flame {r, dwellMs}` fast and wide, `ignition.ember {r, dwellMs}` slower and closer (S1-D049d legibility), dwell accumulates as a rate so stages mix honestly; cooling when out of range (`coolMs`). At kindle: `el.burn = {phase:'burning', fuel: fire.fuelMs·el.s, …}` — **`el.k` and `el.seed` never change; transformed ≠ destroyed**; `e2.ignitions++`. (3) **Burn walk** (`updateBurnLife`): burning→embers→smoke on the fire clocks, then **regrowth** ash→sprout→sapling→itself on `ecology.regrow {ashMs, sproutMs, saplingMs}`; completion deletes `el.burn`, `e2.regrowths++`. Same-seed regrowth: THE SAME tree returns. Burn freezes across sessions like fire's mid-burn checkpoint (save v2 additive `burn` field — Q12 holds, no migration). (4) **Render**: burning matter keeps its body on the world layer, flames/glow/sparks on the burn-through overlay (light, S1-D045); embers pulse, smoke wisps; ash = mound, sprout = shoot, sapling = the base form growing back scaled by phase progress. (5) **Metrics** `e2 {ignitions, regrowths}`; `e1.destructions` stays 0 and every scenario asserts it. (6) smoke17 (fast-clock fixture `eco-ign.json`, smoke11 pattern): gate-off default no-burn; `?e2=1` flame ignition (k unchanged, count stable); full cycle to regrowth; ember-stage ignition + per-stage radius negative (tree inside flame-r but outside ember-r never kindles from embers); sun adjacency never ignites; NO chaining through a burning neighbor; mid-burn reload continuity. smoke11 stays untouched (its fixture has no ignition data — data-gating keeps it true post-flip; only its T1 flag read amends at b3) | code (Claude) | LEAN | — |
| 2026-07-06 | S1-D053 | **M2a-b1 shipped and verified** (`inkblade-m2a.html`, BUILD_ID **S1-M2a-b1-20260706**) — placement choice per S1-D052, exactly as planned: `state.lockExit` recorded at the completing cut (transient, relocks re-aim); `placeEl` scores candidates `(1−pull)·min(spacing,1.4) − pull·distToAnchor` with anchor = exit x in the field, exit y mapped into the kind's band; pull weights shipped as pack data `ecology.placement.pull` = {default 0.3, fire 0.85, seal 0.5} in overrides → both packs; UNIQUE exempt; pull=0 path reproduces the old max-spacing selection bit-for-bit (`worldRand` stream untouched — `?seed=` scenarios all green unchanged). E2 still gated (`E2_ENABLED` false; `__S1_FLAGS.e2` false). Verified smoke16: pull contract in core+basic; same seed, natural vs flicked-through exit → 火's fire plants 0.145/0.093 from each respective anchor (nearest-candidate behavior, input-driven placement demonstrated); 一's horizon byte-identical across exits (UNIQUE exempt). Eyeball: aimed lock puts the campfire at the lower-right where the flick ended, burning through the settled veil — writing reads as aiming. Full battery green: smoke5–16 + frozen smoke1–4, zero console errors; `tests/helpers.js` TARGET_FILE → `inkblade-m2a.html`. Next: b2 ignition+regrowth per S1-D049c/d | code (Claude) | LEAN | — |
| 2026-07-06 | S1-D052 | **M2a-b1 plan — placement choice (OPEN-14 axis per S1-D049b; E2 stays gated until b2+).** The mechanic: the lock's final cutting slash already ends somewhere — that exit point becomes the placement anchor. Because `trailStrokeDist` is a min-distance and matching judges direction classes only, a trail may trace the final stroke and CONTINUE past it toward a spot the player means — the flick-through is free expression, no new input, no judgment (charter §7 Q1 intact). Implementation: (1) 06-combat records `state.lockExit = {x: trailEnd/W, y: trailEnd/H}` at the cut that completes the glyph (transient, never persisted; relocks re-aim). (2) `placeEl` candidate scoring becomes `(1−pull)·spacing − pull·distToAnchor` where anchor = exit x clamped to the field, exit y mapped linearly into the kind's band (drawing high plants far, low plants near — y-as-depth carries into aiming); pull=0 reproduces today's max-spacing selection exactly, tie-break and candidate stream (`worldRand`) untouched → `?seed=` determinism holds, choice varies only with the gesture, which tests control. (3) Pull weights are PACK DATA: `ecology.placement.pull` = {default 0.3 subtle, fire 0.85 decisive, seal 0.5}; engine reads the map, no data → pull 0. UNIQUE kinds (horizon/sun/moon) exempt. Group offsets ride the pulled base (林 groves around your exit). (4) smoke16: pack contract (fire decisive > default); same seed + two different final-stroke exits → 火's fire lands near each respective anchor (input-driven placement); UNIQUE exempt (一's horizon identical across different exits, same seed); verify smoke5/10/12 world assertions (kinds/counts, not positions) stay green via full battery. New artifact `inkblade-m2a.html` (BUILD_ID S1-M2a-b1-20260706); tests/helpers TARGET_FILE follows | code (Claude) | LEAN | — |
| 2026-07-06 | S1-D051 | **M1f-b2 shipped and verified** (`inkblade-m1f.html`, BUILD_ID **S1-M1f-b2-20260706**) — the 撇 grace per S1-D049a/S1-D050, the first ship under full delegation (S1-D049). Mechanism exactly as planned: pie's second real-form center at 180° ±32° in pipeline `CENTERS` and runtime `slashMatches` (new `PIE_FLAT_CENTER`); `classifySlash` untouched. **One plan deviation, found by the pack diff and fixed**: the grace center initially swallowed hookspace — 月/水's terminal up-left hook flicks (~-150°…180°) bucketed as pie, which would harden future non-canon hooks into REQUIRED pie tokens (canon overrides masked it for the emitted packs). Fix: terminal-flick hook detection uses `CENTERS_STRICT` (pre-grace buckets), so hooks stay hooks — optional at play per S1-D034. Final pack diff is surgical: 看手系反笑爱委乎 in, the 8 least-frequent tail chars out (武红响虽推势参希 — next roster tier), **zero verb changes to existing chars, core byte-identical**, queue 70→63 (0 hard; +1 rule-covered 系 chunk merge). New smoke15: all 8 lock end-to-end with real gestures zero-deflect; explicit 170° gold-cuts a pie target and still deflects on heng (grace is pie-only, directional); classifySlash contract re-asserted. **Suite maintenance (extend-not-delete): smoke12 T3 made host-aware** — the session's powersave-throttled desktop failed the absolute 20ms budget on the ALREADY-SHIPPED b1 (24.8ms, reproducible; build innocent, host guilty). T3 now calibrates a breach in-session against frozen `inkblade-m1d.html` (S1-D042's ratified 16.5ms fixture): throttled hosts enforce ≤115% of baseline, capable hosts the absolute budget — code regressions fail on every host. Full battery green: smoke5–15 + frozen smoke1–4, zero console errors (smoke12 passed absolute at 16.51ms in the final run) | code (Claude) | LEAN | — |
| 2026-07-06 | S1-D050 | **M1f-b2 plan — the 撇 grace (implements S1-D049a).** Mechanism: 撇 gains a **second real-form center at 180°** carrying the same ±32° tolerance — S1-D032's tolerance number stays the move-set's only tolerance; no 6th bucket, no new verb for players to learn. Derive: pipeline `CENTERS` gains `['pie', 180]` (nearest-center `bucketOf` unchanged). Match: `slashMatches` accepts a pie target within ±32° of 135° OR 180°. **`classifySlash` stays untouched** — it is the nearest-center classification/metrics contract per S1-D032 (smoke3/smoke5 assert `left→null`; that stands — the grace is target-matching and derivation only). Collision audit: pie's window becomes 103°–212° continuous; nothing else occupies 148°–212° (heng is 0°±32 drawn left→right; ti is −45°; up-left stays hookspace — hook derivation precedes bucketing for terminal flicks, unchanged). Regenerate packs: the 8 hard drops (看手系反笑爱委乎) emit; since basic takes the first 500 survivors of 761, the tail shifts by 8 — expected, frequency-ordered by design. Review the pack diff for chars whose mid-segments at 148°–212° previously absorbed as slivers and now derive as honest pie tokens. New smoke15: the 8 chars present + end-to-end lock with REAL flat-撇 pointer gestures (~170°), a ~170° trace against a heng target still deflects, classifySlash contract re-asserted. Ship as **S1-M1f-b2** (artifact stays `inkblade-m1f.html` — this completes the basic tier), full battery, roadmap update, push | code (Claude) | LEAN | S1-D032 (amends: pie real-form center), S1-D048 (un-drops the 8) |
| 2026-07-06 | S1-D049 | **M2a rulings + complete handoff (John).** From the question round: (a) **OPEN-15 → 撇 real-form grace** — flat-撇 (~170°) is accepted as a real form of 撇 at BOTH derive and match; amends LOCKED S1-D032; restores 看手系反笑爱委乎. (b) **OPEN-14 → placement choice** is M2a's E2 choice axis (final-stroke exit point biases world-mark placement); want-driven repertoire stays M2b. (c) **No chain fires at v1** — ignition propagates one hop from player-planted heat only; sun never ignites; chaining waits behind a pack-data knob. (d) **Embers ignite too** (John chose over the flames-only recommendation) — the ember stage keeps an ignition window; implementation must keep the smolder-catch legible: per-stage ignition params in pack data with ember dwell longer and/or radius smaller than flame. Then the grant: *"you can decide for me. i give all permissions from now on, completely hand off."* — **all future rulings, including LOCKED amendments and open-question calls, are Code's to make**, logged here with rationale; John reads ship summaries. Deepens S1-D040 (no-Chat-track) to full delegation | John | LOCKED | S1-D040 (deepens) |
| 2026-07-06 | S1-D048 | **M1f shipped and verified** (`inkblade-m1f.html`, BUILD_ID **S1-M1f-b1-20260706**). The basic tier per S1-D047: **`packs/basic.json` — 500 characters** (317KB minified, ~634B/char), frequency-ordered roster of 761 authored from corpus knowledge, first 500 derivation survivors emitted, intro = emission order. **8 drops, all one phenomenon**: the flat-撇 opening stroke (看手系反笑爱委乎) at 167–173° sits outside every bucket window — logged as **OPEN-15** (a move-set ruling, not a pipeline bug; honest traces would deflect under LOCKED S1-D032, so no improvised fix). Queue: 70 entries, 62 soft (54 rule-covered sub-2-stroke chunk merges, 8 pre-existing canon diffs), 8 hard = the drops; core still emits clean. **Derivation hardened**: >3-segment strokes now emit `complex:true` (S1-D021's sanctioned escape) instead of dropping; short no-bucket mid-segments absorb as pen-transition slivers. **Radical→class auto-assignment** live (`radicalClasses` data): 122 of the 500 map to families (45 figure, 26 terrain, 23 flora, 17 water, rest curated/tier-1), 378 fall to the seal — every char has a world mark on day one (C3). **Three new tier-2 renderers**, pure brush geometry (no CJK in engine): flora (grass/reed/bush/sprout, shares the heat-shimmer), terrain (rock/boulder/crag), figure (stand/stroll/stoop living agents — beh/hearth/flee apply). **Chapter-pack loader**: `?pack=` comma-list, per-chapter validation with skip-on-failure, first-wins merge (verified: core+basic = 509-char union, curated 火 keeps its mortal tier-1 fire through the dedupe). Verified smoke14: 500 load zero-quarantine + frequency intro + size budget; merge/dedupe/resilience; **six seeded random chars (行活集等华观, up to 12 strokes) lock end-to-end with real gestures, zero deflects**; flora/terrain/figure/seal all plant from locks. Full battery smoke5–13 + frozen smoke1–4 green, zero console errors. Eyeball: families read as ink-sketch siblings under depth staging | code (Claude) | LEAN | — |
| 2026-07-06 | S1-D047 | **M1f plan — the basic tier (~500 chars, path per S1-D043).** (1) **Roster**: `pipeline/roster-basic.txt`, ~510 common simplified chars in approximate corpus-frequency order, authored from general corpus knowledge (an ordering of facts, no licensed compilation copied; MMAH supplies all glyph data) — pipeline emits the first 500 that pass derivation into `packs/basic.json` (id `basic`, intro = roster order → frequency-ordered progression). (2) **Derivation hardened for scale**: >3-segment strokes emit with the sanctioned `complex:true` flag (S1-D021's escape hatch — validator already accepts it) instead of dropping the char; short mid-stroke no-bucket segments (< hook length) absorb as transition slivers (soft-queued); long ones stay hard and drop the char (reported). (3) **Radical→class auto-assignment** (data in overrides: `radicalClasses`): chars without hand-curated `world` map by their MMAH radical to tier-2 families — 水-family→`water` (existing renderer), 木/艹/禾-family→`flora` (grass/reed/bush/sprout forms), 人-family→`figure` (living agents — brush silhouettes, pure geometry, no CJK in engine), 山/土/石-family→`terrain` (rock/boulder/crag); unmapped→`world:null`→seal (C3 covers all 500 on day one). Form picked by codepoint — siblings, not clones. New kinds: figure{agent,living}, flora{structure,flammable}, terrain{structure}. `fx:null` for auto chars (drawEffects falls through safely). (4) **Chapter-pack loader**: `?pack=` accepts a comma-list; each URL validated independently (bad ones skipped with warning), merged first-wins — meta/intro from the first pack, kinds/ecology first-wins, chars deduped by ch. Embedded core stays the default game. (5) smoke14: 500 load + zero quarantine, merge/dedupe, seeded random-sample locks with real gestures, class world-presence, frequency intro, pack-size budget | code (Claude) | LEAN | — |
| 2026-07-06 | S1-D046 | **M1e shipped and verified** (`inkblade-m1e.html`, BUILD_ID **S1-M1e-b2-20260706**; lineage b1→b2, b2 = heatAuraR 0.07→0.10 so the aura reaches past warmDist 0.09 and the warming walker actually gains the hearth pose — found by smoke13 T3). Contents per S1-D045: **events burn through the veil** — live heat (the `heatSources` predicate, now shared as `isLiveHeat`) and its spark particles draw on a second overlay composited at `max(worldAlpha, EVENT_MIN_ALPHA=0.85)`; figure-ground for everything else untouched. **Fire clocks retuned** (pack data): 90s fuel / 20s embers / 15s smoke / 3min ash-heal / flares 7–18s — full cycle ≈ 2 minutes, observable in one sitting. **Spectacle**: third flame tongue, glow radius +44%, spark fountains while flaring. **Hearth interactions** (E1-safe, aura + tags in pack data): `hot` transient cue → trees shimmer (heat haze), water steams (`wet` tag new on water kind), warming walkers stand hearth-calm; comfortR 0.16→0.22. Verified: smoke13 — pack clock contract; pixel evidence off the live canvas that during settled writing recession (worldAlpha 0.35, real pointer events) the fire keeps ~950–1300 fire-colored px while the tree drops to 0, and bloom restores both (the playtest complaint, inverted into a regression); hearth scene — tree/water/walker all hot, steam risen, sparks shed, walker warmed at exactly warmDist, 4/4 elements alive, destructions 0. Full battery green: smoke5–12 + frozen smoke1–4, zero console errors. Roadmap file `inkblade-roadmap.md` created per S1-D044 | code (Claude) | LEAN | — |
| 2026-07-06 | S1-D045 | **M1e plan — "the fire burns through" (playtest response, S1-D044).** (1) **Events burn through the veil**: world render splits into two composite passes — everything composites at `worldAlpha` exactly as today (S1-D019 figure-ground unchanged for structures/agents), but live heat events (heat tag + life in `heatPhases` — the same predicate as `heatSources`) and their spark/flame particles composite at `max(worldAlpha, EVENT_MIN_ALPHA≈0.85)`. Rationale: fire is *light*; the veil is paper, and light reads through paper. The playtest's "effect stopped" is exactly this bug in the attention model's interaction with ongoing events. (2) **Fire clocks retuned in pack data** so one sitting holds a full cycle: fuel 360s→90s, embers 45s→20s, smoke 30s→15s, ash-heal 8min→3min, flares every 7–18s (was 15–40s). (3) **Burning made spectacular**: third flame tongue, larger glow, spark bursts on flare. (4) **New E1 interactions, all reactive/additive** (nothing destroyed; radii in pack): `heatAuraR` — a transient `hot` cue the sim stamps on elements near live heat; hot **trees shimmer** (fast sway — heat haze, not damage); hot **water steams** (wisp particles; `wet` tag added to water kind); warming **walkers stand hearth-calm** (sway stills while warmed — a gathering, not a walk-by); comfort radius widened 0.16→0.22 so more walkers join the hearth. (5) smoke13: punch-through pixel evidence writing-vs-bloom, hot-cue/steam/hearth assertions, pack clock contract, invariant. Regrowth (ash→sprout) noted for M2 in the roadmap, not here | code (Claude) | LEAN | — |
| 2026-07-06 | S1-D044 | **Playtest findings + router priority (John).** Findings: (a) burning is not visually obvious — smoke visible, but the writing veil (attention recession) makes the effect *stop* whenever a glyph is in the foreground; (b) no full fire cycle was ever observed in play (shipped clocks are minutes long and the world is receded most of the time); (c) burning is the most promising mechanic — **prioritize fire-centered interactions**. Ruling: fire visibility + interactions jump the queue ahead of roster scale-up. Also mandated: since there is no Chat/design track, **a roadmap file must live in the repo** (`inkblade-roadmap.md`) recording planned releases so any future session resumes without loss | John | LOCKED | — |
| 2026-07-06 | S1-D043 | **Scalability target set (router): the game should ultimately hold the common daily lexicon — 3,000–7,000 characters** (common basic ≈500 as the nearer waypoint). Assessed path, all within existing architecture: (1) **data** — vendored MMAH covers 9,574 chars with medians+order, full headroom; pack v2 costs ~566 bytes/char (500 chars ≈ 280KB, 3,000 ≈ 1.7MB, 7,000 ≈ 4MB), so the single-file embedded pack carries the ~500-char tier comfortably and larger tiers ship as fetched **chapter packs** over the existing `?pack=` machinery (multi-pack merge = small loader extension, schema unchanged); (2) **curation** — the pipeline already auto-derives verbs/pts/comps; the ambiguity queue triages only flagged chars, and per-char cost falls with each batch rule (hook rule, real-form precedent). pinyin/gloss automate from the vendored dictionary; (3) **world presence** — the C3 tier system is the scaling answer: every char has a mark *today* via seal fallback (world:null); tier-2 class families keyed by semantic radical (氵→water, 木/艹→flora, 亻→figure, 山→terrain, …) give thousands of chars distinct-sibling looks from a few dozen renderers + params + seed variance; the bespoke tier-1 set stays small and poetic; (4) **capacity** — WORLD_CAP 300 at 60fps measured (S1-D042), relocks reuse elements, seals accumulate as a real scroll's do. No engine change is required to grow the roster — growth is pack data + renderer library, which is the architecture working as designed | John (target) + code (path) | LEAN | — |
| 2026-07-06 | S1-D042 | **M1d shipped and verified** (`inkblade-m1d.html`, BUILD_ID **S1-M1d-b1-20260706**). Contents per S1-D041: **depth staging** — y-as-depth projection (`depthK`: far 0.55× → near 1.25×, anchor-scaled so line weights scale too; horizon/sun/moon exempt), painter's sort sky→far→near so near occludes far, paper-tone mist band over the far zone (atmospheric perspective); **wider bands + 2D-spaced placement** (candidate (x,y) pairs scored by 2D same-kind distance; grouped spawns hold the base's depth ±jitter — 林 stays one grove); **WORLD_CAP 80→300**; **`__M0_METRICS` sunset** (S1-D028 debt paid; smoke6 assertion inverted to guard the sunset). **C4 perf measurement at cap: 300 elements = 16.5ms avg / 17ms worst frame (60fps, headless Chromium)** — pre-render machinery not demanded, and would trade away the living scroll's sway/ripple animation; deferred unless a real device measurement breaches. No save migration: y already carries depth (v2 unchanged, Q12 trivially holds). Verified: smoke12 — projection contract (far<1<near, sky exempt), near-paints-more-ink pixel evidence off the live canvas (17px far vs 5,480px near), 300 elements co-exist at budget, E1 startle across a pure-y separation (2D danger distance) with destructions=0; full battery smoke5–11 + frozen smoke1–4 green, zero console errors. Visual eyeball via seeded screenshot: mist-veiled far peaks, scaled walkers, near-occludes-far confirmed | code (Claude) | LEAN | — |
| 2026-07-06 | S1-D041 | **M1d plan (depth staging + capacity + maintenance).** (1) **Depth staging** [LEAN, presentation only]: the scroll's y-axis *is* its depth axis (shan-shui convention: higher on the paper = farther) — no save migration, v2 unchanged. Render-side projection: per-element scale ∝ depth (far ≈0.55×, near ≈1.25×, anchor-scaled at the element's ground point), painter's sort far→near so near occludes far (sky draws first; horizon/sun/moon depth-exempt), atmospheric mist band over the far zone. Constants in 00-config. (2) **Wider placement bands + 2D-spaced placement**: ground kinds spread across the depth field (placeEl scores candidate (x,y) pairs by 2D distance to same-kind neighbors; grouped spawns keep members at the base's depth ±jitter); `elDist` is already 2D, so E1 interactions gain the depth dimension with zero sim changes — reaction tuning stays pack data. (3) **WORLD_CAP 80→300** (the C4 soft-budget number) with frame-time measured at cap; per S1-D031, pre-render machinery only if measurement demands it — noting structures animate by design (sway/mist/ripple), so naive static pre-render would trade away the living scroll. (4) **`__M0_METRICS` alias removed** — S1-D028 scheduled removal at M1b, two versions overdue; smoke6's alias assertion inverts to assert the sunset. (5) New smoke12: depth projection contract, near-occludes-far pixel evidence, 300-element cap + perf, ecology regression at depth-spread placements | code (Claude) | LEAN | S1-D028 (alias sunset) |
| 2026-07-06 | S1-D040 | **Router ruling: there is no Chat track — this game is lightweight, lean management.** Code decides next tasks on its own recommendations; design deliverables previously marked "Chat" (OPEN-9 demand world, OPEN-14 E2 choice axis) reassign to Code when their milestones arrive. One standing enhancement request from John: **make the world read as 3D** — depth so that more creations co-exist and more interactions can take place | John | LOCKED | S1-D034 (routing), handoff §8 (Chat-scoping) |
| 2026-07-06 | S1-D039 | **S1-D038 (M1c ship) RATIFIED by router.** The M1 code handoff is fully delivered: workstreams A (M1a), C1/C2/C3/C4 (M1a–M1b), B (M1c) all shipped and ratified. M2 is design-gated on Chat deliverables (OPEN-9 demand world, OPEN-14 E2 choice axis) — out of Code's scope per handoff §8 | John | LOCKED | — |
| 2026-07-06 | S1-D038 | **M1c shipped and verified** (`inkblade-m1c.html`, BUILD_ID **S1-M1c-b2-20260706**; lineage b1→b2, b2 = flare-time mid-burn save checkpoint). Contents: **ecology E1** (S1-D020, handoff workstream B) — all reaction tuning lives in the pack `ecology` block (comfort/danger/warm/flee/shelter radii, walk/flee speeds, rest clocks, fire fuel/ember/smoke/flare/ash timers); the engine knows tags (`heat/living/shelter/mortal`), never numbers. **R4 fire mortal lifecycle**: burning (fuel ∝ size, flares on a randomized clock) → embers → smoke → ash → healed; ash decays by wall clock across sessions; `life` persisted in save v2 (additive field — Q12 holds, old saves untouched); flares double as mid-burn save checkpoints so a reload resumes remaining fuel rather than restarting the burn. **R2 startle/flee/return**: a walker inside the danger radius (flare-boosted) startles, hops, flees past fleeDist, avoids while the fire gives heat, and walks home after it dies. **R1 comfort-radius warming**: walkers drift to warmDist and loiter, one warming counted per episode. **R3 shelter rest**: sheltered walkers take occasional rest beats with cooldown. **E2 stays HARD-GATED** (`E2_ENABLED=false`, `window.__S1_FLAGS.e2` test-readable; no ignition code exists to enable — OPEN-14 unchanged). Rendering: phase-aware fire (charred logs, pulsing ember glow, smoke wisps, fading ash), walker rest/startle-hop poses; world events never initiate AudioContext. Verified headless Chromium, real dispatched pointer events, deterministic seeded scenarios (fast-clock fixture pack — retuning the world touched zero engine code, which is itself the data-driven proof): smoke11 — E2 flag off; REAL 火 lock → full lifecycle burning>embers>smoke>ash>healed, burnouts=1; seeded walker inside dangerR startles+flees away and returns home after burnout while the adjacent tree survives the entire fire lifetime; warming drift+loiter with zero startles; sheltered rest; mid-burn checkpoint round-trips remaining fuel through reload; stale-wall-clock ash heals at load while fresh ash survives; **e1.destructions === 0 in every scenario (E1 invariant)**. Full battery green on this build: smoke5–10 + frozen smoke1–4, zero console errors everywhere | code (Claude) | LEAN | — |
| 2026-07-05 | S1-D037 | **Router ratifications:** S1-D032 (target-tolerance matching) APPROVED — status upgraded to LOCKED. OPEN-13 CONFIRMED: depth-1 component chunking stands, with the M1a leaf-chunking refinement accepted (chunk by leaves of the char's own decomposition tree; 墨 = 黑+土, never recursive). M1c (ecology E1) authorized | John | LOCKED | — |
| 2026-07-05 | S1-D036 | **M1b shipped and verified** (`inkblade-m1b.html`, BUILD_ID **S1-M1b-b1-20260705**). Contents: **C1 polyline verb model** — runtime trail corner-split classifier (constants numerically shared with the pipeline's derivation: 50° windowed turn, 0.14S window, 0.035S step, max 2 splits), bucket-string matching per segment with S1-D032 target tolerance, hooks optional per S1-D034 (omitted hook matches; one extra terminal flick accepted unjudged), `complex` leniency (first+last) plumbed; **C3** — seal-on-lock exercised (world:null chars), tier-2 class families with per-char skin params persisted on elements (`p` field, additive to save v2); **extended pack: 口日月田国土山水川** (23 chars total) with the sky axis unlocked (unique sun/moon per S1-D017's gate — the world gains a sky because the move-set did), field/water renderers; queue 11 soft/0 hard, all reviewed (data notes: 日's fold carries no hook in canon — my initial entry corrected; 川 stroke 0 drawn at 102.6°, outside pie's ±32 window → real-form `shu` per the S1-D014/焱 precedent; 月 stroke 0 at 104.4° stays canon `pie`, inside tolerance). Verified headless Chromium, real dispatched pointer events: smoke9 — all 9 new chars lock end-to-end with real fold/hook gestures, zero deflects; straight `shu` cuts 水's 竖钩 (hook-optional); deliberate `heng>pie` wrong-turn on 口's 横折 deflects; sinusoidal-jitter straight slash never splits (cuts gold); smoke10 — 口 plants a seal bearing its form; 水+川 plant two `water` elements with distinct persisted skins; 山 raises a peak; 日×2 = one unique sun; smoke5–8 full regression green on this build; frozen smoke1–4 green. Zero console errors everywhere. OPEN-2 is now fully CLOSED in implementation, not just design | code (Claude) | LEAN | OPEN-2 (implementation) |
| 2026-07-05 | S1-D035 | **OPEN-12 resolved: stay on MMAH under the 1999 Arphic Public License; no independent alternative exists.** Investigation findings: every open PRC-standard stroke DB with medians+order coverage is Arphic-derived (Hanzi Writer data and AnimCJK svgsZhHans are both MMAH/Arphic lineage); the only independent vector DB is KanjiVG (CC BY-SA) — Japanese glyph forms and stroke conventions, unusable for PRC stroke-order pedagogy; Wikimedia Stroke Order Project is bitmap/incomplete. Decisive: MMAH ships the **1999 APL, FSF-recognized free copyleft permitting commercial use** (the non-commercial variant is the separate 2010 revision, not used by MMAH). Compliance path: packs are APL-licensed data artifacts (copyleft attaches to font-derived data, not the engine — the engine/content split already enforces the boundary); `ARPHICPL.TXT` vendored verbatim at `pipeline/data/APL.license` and pack `meta.source` carries the notice; dictionary-derived fields (pinyin) fall under MMAH's LGPL/Unihan side, also vendored. If a future commercial step demands non-copyleft data: license commercially or commission — logged as the known trade, not a blocker. Per S1-D034, M1b is unblocked | code (investigation) + John (gate) | LEAN | OPEN-12 |
| 2026-07-05 | S1-D034 | **Router rulings batch (2026-07-05):** (1) S1↔R4 relationship clarified — Inkblade has nothing to do with the sandbox game; only the game world may *resemble* a sandbox full of creatures. The S1-D020 kinship note stands as a cosmetic observation; no further cross-log obligation is tracked by Code. (2) **OPEN-11 CLOSED: hook matching is OPTIONAL** — a straight `shu` slash cuts a 竖钩; performing the hook is polish, never required, and never punished. (3) OPEN-12 investigation delegated to Code ("find another stroke DB if any"); **M1b gates on OPEN-12 resolution** and is authorized once resolved | John | LOCKED | OPEN-11 |
| 2026-07-05 | S1-D033 | **M1a shipped and verified** (`inkblade-m1a.html`, BUILD_ID **S1-M1a-b5-20260705**; lineage b1→b5). Contents: modular source (14 concat-built modules, dependency-free `tools/build.mjs`) · pack v2 pipeline-generated from MMAH medians (14 chars; ambiguity queue 6 soft entries, all spot-reviewed: 二/三 sub-2-stroke chunk merges + 4 steep-撇 canon pins) · save v2 migration with one-time backup and unknown-kind→seal self-heal · attention veil/recession/bloom · `__S1_METRICS` (+`__M0_METRICS` alias). Verified headless Chromium, real dispatched pointer events: smoke5 = complete smoke1–4 scenario port with geometry-derived slashes — 14/14 canonical locks on generated data with zero deflects, wrong-order fizzle, deflect, whiff, classification contract, 森 component-break classification (3 comps, ash=3→gold=3), hint ladder, full world battery; smoke6 = writing 0.35 / bloom 1.0, veil present-then-absent, hold, glyphLog instrumentation; smoke7 = `?pack=` override locks end-to-end, future pack version rejected cleanly, corrupted stroke quarantined alone, missing pack falls back; smoke8 = implicit-v1 save incl. unknown kind migrates losslessly, backup byte-identical, v2 round-trip; frozen smoke1–4 re-run green on their frozen builds. Defects found by the suites and fixed: `window.BUILD_ID` unset (b1→b2), single-char-pack advance crash (b3→b4), first-glyph hint char literal in engine — now `meta.intro[0]` + single-stroke rule (b4→b5). Zero console errors everywhere | code (Claude) | LEAN | — |
| 2026-07-05 | S1-D032 | **Target-tolerance matching (extends S1-D013) + real-form verbs.** Real medians expose two facts hand data hid: steep 撇 sit at ~110° (inside the shu/pie tolerance overlap — nearest-center matching would deflect an honest along-stroke slash on 大/火/炎/焱), and 焱's compressed bottom-fire first dots are drawn fully vertical. Rulings: (a) a slash matches a stroke when its angle is within ±32° of the **target's** bucket center — the target's own type disambiguates overlap zones, the exact S1-D013 pattern generalized; `classifySlash` (nearest-center) unchanged as the classification/metrics contract; anti-grading invariants intact — direction classes only, nothing reads curvature/proportion/neatness. (b) 焱 strokes 4/8 carry verb `shu` per their real drawn form — positional inflection honored in data (S1-D014) and real data supersedes the hand-authored 45° approximation (the OPEN-7 retirement working as designed). (c) `pickNext` no longer crashes on single-char packs (prevCh exclusion waived when the pack has one char; found by smoke7). Implemented and verified in S1-M1a-b3+; **(a) is a mechanics ruling made under the router's "decide for me" grant — ratification requested** | code | LEAN | S1-D013 (scope) |
| 2026-07-05 | S1-D031 | **M1a lands in two verified steps, two BUILD_IDs.** Step 1 `S1-M1a-b1`: refactor + save v2 + veil + metrics rename with hand-authored data carried verbatim (bit-identical stroke geometry) so smoke3(sed)+smoke4 gate it directly. Step 2 `S1-M1a-b2+`: pipeline-generated pack swaps in, smoke5–8 gate. Perf: particle caps at M1a; offscreen pre-render of "static" structures DEFERRED behind a frame-time probe — as written it conflicts with S1-D017 (everything idles forever; no structure is static); low-rate (~10Hz) offscreen invalidation is the fallback mechanism if the probe shows pressure at 300 elements. Ruling per router 2026-07-05 ("decide for me") | code, confirmed John | LEAN | — |
| 2026-07-05 | S1-D030 | **Regression strategy for generated data.** smoke1–4 are frozen against the frozen builds and never edited (path adjustment per suite README excepted). New suites: smoke5-regression (all smoke1–4 scenarios ported to a runner that reads live stroke endpoints from exposed state and dispatches real pointer slashes along them — decouples tests from authored geometry; direct handler invocation remains banned), smoke6-veil (attention state assertions), smoke7-packs (validator + version gate + ?pack=), smoke8-migration (v1 payload incl. unknown kind → survives, seals, round-trips as v2, backup written). Every suite asserts BUILD_ID + zero console errors. Router confirmed "you can fix the regression tests" | code, confirmed John | LEAN | — |
| 2026-07-05 | S1-D029 | **Content pipeline (offline).** `pipeline/build-packs.mjs`, authoring-time only: MMAH ingest (vendored, OPEN-12 prototyping only) → normalize to 0–100 y-down box, resample ≤12 pts/stroke → verb derivation by cumulative-turn corner split (~50°, max 2 splits; same constants the M1b runtime classifier will share from config) + ±32° classification → depth-1 comps (OPEN-13) with stroke-count reconciliation → ambiguity queue for manual review (generated data `[LEAN]` until spot-verified; retires OPEN-7) → versioned pack emit. Pinyin/gloss/fx/world at M1a from hand-curated `overrides.json` (14 chars); Unihan/CC-CEDICT automation deferred to M1b. Acceptance = regenerated v1 roster passes the full suite per S1-D030 | code, confirmed John | LEAN | — |
| 2026-07-05 | S1-D028 | **Metrics rename + extension.** `window.__S1_METRICS` canonical; `window.__M0_METRICS` aliases the same object for exactly one version (removal flagged for M1b). Adds `glyphLog` ring buffer (per-glyph ch/worldDensity/timeToFirstCut/whiffs, cap 200), `world.density`, zeroed `e1` counters incl. `destructions:0` — the field existing and asserting 0 is itself the E1-invariant regression hook | code, confirmed John | LEAN | S1-D007 (naming) |
| 2026-07-05 | S1-D027 | **Attention choreography implementation (S1-D019).** Mode machine driven by game state: writing (glyph active) / bloom (lock-fizzle through resolve; bloom holds ATTN_HOLD_MS=1500 into the next glyph before receding over 400ms — cadence-preserving reading of "holds ~1.5s after", keeps every suite timing intact) / idle (title). Recession: world drawn to an offscreen layer composited at worldAlpha (0.35 writing / 1.0 bloom) — exact global-alpha semantics per the spec. Veil: soft radial panel behind glyph bbox+margin, paper +6%, alpha 0.5. All numbers `[LEAN]` in config. Test-readable: `window.__S1_ATTN`. Per-glyph instrumentation: worldDensity, timeToFirstCut, whiffs. Ambient audio: hook defined at M1a, no possible caller until E1 — implementation M1c (router: audio lower priority) | code, confirmed John | LEAN | — |
| 2026-07-05 | S1-D026 | **Save schema v2 + append-only migration.** Payload `{version:2, els:[{k,x,y,s,seed,cls}]}`; key stays `inkblade_world_v1` (payload carries version; missing field = implicit v1). Migrations = append-only pure functions, run in memory on load, persisted on next save; raw pre-migration payload copied once to `inkblade_world_v1_backup`. Unknown kinds carried forward untouched — self-healing lives in render (seal fallback), never in migration; nothing is ever dropped (Q12). Minimal seal renderer ships at M1a for the fallback (router ruling on the C3/M1a spec collision: "decide for me" → seal early); full seal-on-lock rule stays M1b | code, confirmed John | LEAN | implicit-v1 format |
| 2026-07-05 | S1-D025 | **Pack delivery.** Default pack embedded at build time as a JSON script block (runtime fetch of sibling JSON is CORS-blocked on file://, where suites and players load); engine source contains no pack content — the build step injects the pipeline's emission verbatim. `?pack=url` fetches an override at runtime; `?seed=` routes world randomness through the seeded rng for deterministic ecology tests (plumbed at M1a for M1c); `?char=`/`?reset=1` unchanged | code, confirmed John | LEAN | — |
| 2026-07-05 | S1-D024 | **Character-pack schema v2 (final for M1).** Strokes `{verbs, pts[, form]}`: `pts` (0–100 box, ≤12 points) sole source for render+hit; `verbs` (`>`-joined over heng·dr·shu·pie·ti + terminal hook, ≤3 segments, `complex`=lenient first+last) sole source for matching; per-stroke dian/na type retired — S1-D013 falls out of `dr` + geometry (`form` is a legacy render hint for hand-authored 2-pt strokes only, dropped when real medians land). Adds over handoff draft: `fx` (meaning-effect id+params), multiplicity folded into `world.params` (deletes the 14-case switch), `meta.intro` (curriculum order out of engine), `kinds` block (element class/tag assignments — S1-D020 "assignments in data"). `world`: tier-1 kind / tier-2 class / null→seal. Unknown `version` rejected cleanly; invalid chars quarantined with visible warning | code, confirmed John | LEAN | handoff §6 draft |
| 2026-07-05 | S1-D023 | **M1a module layout + build step.** Source split: config (all tuning, `[LEAN]`) · state/metrics · data (pack load/validate, zero inline content) · canvas/geometry · audio · input/classification · glyph combat · fx · persistence · world sim · world render · attention · render · main. Build = dependency-free Node concat (`tools/build.mjs`) into one classic-script HTML (file:// clean, suite-compatible top-level globals preserved: `classifySlash`, `state`, metrics); BUILD_ID stamped from one source into header + on-canvas + `window.BUILD_ID`. Engine contains zero character data, element mappings, or reaction tuning (Q11) | code, confirmed John | LEAN | — |
| 2026-07-05 | S1-D022 | **v1 declared DONE by router** (base build S1-M075-b2-20260705, S1-D001–D021 standing). M1 work order issued to Code as `inkblade-m1-code-handoff.md` (workstreams: attention choreography · ecology E1 · extensibility platform). Charter remains single source of truth; handoff is derived and subordinate | John + chat | LOCKED | — |
| 2026-07-05 | S1-D021 | **Extensibility verdict: current spec is NOT infinitely extensible; four upgrades make it lexicon-complete (~9.5k chars).** (1) Polyline verb model — a stroke's verb is a *sequence* of direction buckets (横折=`heng>shu`), corner-split trail, ≤3 segments, direction classes only, anti-grading invariants LOCKED, hooks optional (OPEN-11), new `ti` bucket; unlocks 口日月田国 and the lexicon, closing OPEN-2's design. (2) Content pipeline — offline build step deriving character packs from a stroke DB (medians→geometry, auto verb-classification with ambiguity review queue, depth-1 component chunking OPEN-13); closes OPEN-4, retires OPEN-7; licensing review flagged (OPEN-12). (3) World element classes + **seal fallback rule [LOCKED]: no character locks without leaving a mark** — unmapped chars plant as cinnabar seals/steles (diegetically native; makes the world extensible by construction; unknown save-kinds self-heal to seals). (4) Data-driven modular architecture, versioned saves with migrations (never destroy a player's world), render budget. All v1 systems survive as the base layer | chat | LOCKED | — |
| 2026-07-05 | S1-D020 | **Ecology adopted in two phases with one hard gate.** Corrected sequencing insight: destructive ecology *without a choice axis* forces the player to torch their own garden (roster dictates 火; auto-placement plants it; trees burn; punishment without agency) — so Level 2 depends on Level 3's choice ingredient. **Matter classes** named: structure (permanent) / agent (mobile) / event (mortal — fire burns out to embers→smoke→fading ash; the scroll remembers, then heals; solves cap-saturation with meaning). **E1 (ADOPT, M1):** reactive ecology — agents warm at/flee fire, rest under trees; fire lifecycle; nothing player-planted is ever destroyed. **E2 (SPEC'D, HARD-GATED behind default-off flag):** heat ignites flammable, spread depth 1, ash scars — ships only after placement or want-driven repertoire choice exists (OPEN-14). Kinship honesty: tag-reaction machinery is public-domain sim tech (Objectnaut lineage), not R4's identity (R4's world is glyphs; this scroll is meanings) — router to add one-line cross-reference in R4 charter per OPEN-10 discipline. Dark-twin honesty: ecology alone is decoration; it becomes identity-bearing at Level 3 when knowing 水 is how you answer a fire | John + chat | LOCKED | OPEN-10 |
| 2026-07-05 | S1-D019 | **Attention choreography (the figure-ground contract) [LOCKED principle]:** at any moment exactly one visual plane owns attention; game state drives transitions — writing→glyph owns, resolution→world owns. Mechanism: focus recession (world to ~35% ink presence while a glyph is active, animation continuing) + writing veil (soft fresh-paper panel behind the glyph — a leaf laid over the scroll) + bloom (world to full vividness through lock/fizzle + celebration beat). Rejected: adaptive ghost contrast (corrupts the ink language), permanent spatial partition (kills the filled-canvas dream), distraction-pulse (conflates distraction with confusion). Converts the legibility bug into the loop's breathing rhythm; the bloom beat is where the future demand world speaks. Effectiveness instrumented: timeToFirstCut & whiff rate vs worldDensity | John + chat | LOCKED | — |
| 2026-07-05 | S1-D018 | **M0.75 build shipped and verified** (`inkblade-m075.html`, BUILD_ID S1-M075-b2-20260705). Verification via real dispatched pointer events: 木×3 plants three individualized trees (distinct seeds/sizes) with relocks=2; world persists across page reload; 一×2 replaces the unique horizon (no duplicate); 森 plants three trees in one lock; 林 two; 众 one walking crowd; fizzle plants nothing; full M0.5 suite re-run against this build with zero regressions; zero console errors. One defect found and fixed in b1→b2: elements planted between frames had `born` ahead of the next rAF timestamp → negative grow-in progress → negative ellipse radius; progress now clamped ≥0 | code (Claude) | LEAN | — |
| 2026-07-05 | S1-D017 | **Living-scroll spec** (router rulings folded in): the world must be *alive* — walkers amble with per-instance gait/span/direction-turn, trees sway and shed leaves, fires flicker and spit embers, mist drifts across peaks, the 休 figure breathes. Duplicates encouraged: every instance is a per-seed individual; first-ever lock of a character plants with a gold ripple flourish, repeats plant quietly. No session-length metric; the artifact is an animated ink painting. Sky axis (日/月, day/night) honestly gated on OPEN-2 fold strokes rather than faked | John + chat + code | LEAN | — |
| 2026-07-05 | S1-D016 | **Persistence adopted — not as decoration but as the seed of the demand loop.** Framings ladder ruled: Level 0 trophy garden (ADOPT as floor; Fun Gate permits juice), Level 1 vocabulary leverage + cross-session persistence + repetition density (ADOPT — this build; 森=3 trees makes knowledge visibly leverage), Level 2 ecology (DEFER — R4 tag-reaction kinship, needs dual-log ruling, OPEN-10), Level 3 demand world (DEFER — M1 headline, OPEN-9: world wants replace some ghost glyphs, turning production from copying into composing; solves OPEN-3). Doctrinal grounding: persistence is the stability rule extended from glyph to world — stable persists, fizzle leaves nothing. Identity extension asserted: **your literacy is the landscape** — the world is a diegetic corpus of what you can write. Auto-composition into shan-shui bands; player placement deliberately excluded until the Level-3 choice layer | John + chat | LOCKED | — |
| 2026-07-05 | S1-D015 | **M0.5 build shipped and verified** (`inkblade-m05.html`, BUILD_ID S1-M05-b1-20260705). Verification via headless Chromium + real dispatched pointer events: all six composites lock canonically (incl. compressed 点 gestures); 森 with wrong component order but correct internal orders fizzles with `orderBreaks.component=1, internal=0` and 3 ash mini-completions, then locks on canonical retry with 3 gold mini-locks; hint ladder fires idle→breathe then flail→comet and the character still locks after hints; singles regression (一人木, crossing case, whiff, classification) clean; zero console errors across all pages | code (Claude) | LEAN | — |
| 2026-07-05 | S1-D014 | Composite roster 休众林炎森焱 adopted (6→12 strokes, all bucket-legal, built from taught primitives 人木火); curriculum intro order 一人木火→composites; **positional inflection honored in stroke data** — 林 left 木 and 炎 top 火 compress 捺→点 per standard orthography, so the required gesture physically changes with position (production-side positional grammar, native and free). 众/焱 bottom-left compressions follow the same pattern, flagged for verification (OPEN-7). 墨 excluded: fold strokes gate on OPEN-2, not on the composite system | chat + code | LEAN | — |
| 2026-07-05 | S1-D013 | **Merged down-right verb**: 点 and 捺 are one slash direction (`dr`); the target stroke's own type disambiguates. Supersedes S1-D007's absolute-length split, which composite compression breaks by construction (a compressed 捺 falls below any fixed threshold → deterministic false deflects — the S1-D009 smoke pass held only at single-character scale). Nothing is lost: lock/fizzle never graded length, and the visible stroke forms still differ | chat + code | LOCKED | S1-D007 (length split) |
| 2026-07-05 | S1-D012 | **Breathing hint** adopted (John's 3-second nudge instinct, retargeted from sprite to substrate): triggers on flail (2 consecutive non-gold resolutions — typically <3s of being wrong) or idle (6s without a cut; 3s punishes legitimate scanning of 12-stroke ghosts). Level 1: the component containing the next canonical uncut stroke breathes ~2.6s. Repeat trigger: comet along that exact stroke. Never pre-emptive, never an external marker, composites only. Thresholds are constants at top of file | chat + John | LEAN | — |
| 2026-07-05 | S1-D011 | **Fractal combo structure** adopted and named (original contribution): global canonical order already equals component order × internal order, so composites need no new rule — only chunked presentation and chunked feedback. Component wash (faint per-component ink-wash tint — the character's own anatomy as the segmentation visual, always on: segmentation is perception support, order is the skill) + mini-locks (all-gold component inks solid with pulse+chime; any ash settles grey — errors localize by chunk and by level). A composite character is a combo of combos | chat | LOCKED | — |
| 2026-07-05 | S1-D010 | **Sprite/pictorial component markers REJECTED** (octopus-style overlays, Chineasy lineage): dark-twin fails — reskin the glyphs and the marker still says "attack here," so skill attaches to reading markers, not character anatomy; always-on order guidance deletes the component-grammar layer entirely. The needs behind the proposal (segmentation, failure-triggered guidance) are real and are met by S1-D011/D012 in substrate terms | chat | LOCKED | — |
| 2026-07-04 | S1-D009 | **M0 build b2 shipped and verified** (`inkblade-m0.html`, BUILD_ID S1-M0-b2-20260704). Verification via headless Chromium + real dispatched pointer events (puppeteer mouse), never direct handler calls: full 8-character roster locks in canonical order end-to-end; wrong-order run fizzles; wrong-direction slash deflects; whiff leaves state untouched; dian/na/left/up classification verified; zero console errors across both suites; 木 (max stroke-crossing) locks with zero spurious deflects | code (Claude) | LEAN | — |
| 2026-07-04 | S1-D008 | **Alignment-priority targeting**: a slash cuts the stroke it runs along, not the stroke it crosses — bucket-matching hits win; distance breaks ties. Root cause found in b1 via live event tracing: any slash crossing a perpendicular stroke intersects it at distance exactly 0, while pointer quantization leaves the parallel target at ~0.4px, so min-distance targeting deflected every horizontal slash across 十. Class of bug: selector metric misaligned with player intent | code (Claude) | LOCKED | b1 targeting |
| 2026-07-04 | S1-D007 | M0 build rulings: five buckets live with dian/na split by length (< 0.34×glyph size = dian); roster 一二三十人大木火 in fixed intro order then weighted-random favoring unlocked; gesture hint on first glyph (一) only — teaches slash, cannot leak order; one framing line on boot screen is the permitted text maximum, zero in-game text; BUILD_ID fingerprint in header + on-canvas + `window.BUILD_ID`; playtest metrics exposed at `window.__M0_METRICS` | chat + code | LEAN | — |
| 2026-07-04 | S1-D006 | PvP parked to M2+. Logged risk: literacy asymmetry as competitive arsenal — identity-pure and a matchmaking problem simultaneously | chat + John | LEAN | — |
| 2026-07-04 | S1-D005 | 形声 trap does not exist on this track: order grammar applies to every character equally; meaning is granted on completion, not decoded from form. Vocabulary effectively unbounded — required by the novelty-pressure law | chat | LOCKED | — |
| 2026-07-04 | S1-D004 | Novelty-pressure law adopted (Law 2): winning skill must be order grammar, not roster memorization; content pipeline must outpace memorization. M0's 8-char roster exempt (tests the loop, not the pipeline) | chat | LOCKED | — |
| 2026-07-04 | S1-D003 | **John's reactive framing adopted, superseding the open-sandbox M0**: ghost glyph presents a real character's strokes as targets; discovery collapses to "find the order," solvable by grammar. Variant A (shape-grading) REJECTED as the dictation game with juice; Variant B (direction-matched slashing) ADOPTED — direction buckets are real orthography, coarse enough to never feel graded, and the target's visible form tells the player the required gesture | John + chat | LOCKED | pre-charter open-sandbox proposal |
| 2026-07-04 | S1-D002 | Stability rule imported from the Lab's shared principles: sequences that match the real lexicon LOCK (assemble, speak, manifest meaning); broken order FIZZLES (crumble, still "did a thing"). "Wrong" is never surfaced; gold/ash per-stroke ink is the only order feedback and the entire pedagogy | chat | LOCKED | — |
| 2026-07-04 | S1-D001 | Combo-orthography model adopted: identity claim per §1.1; dictation game named as the anti-pattern; original contribution asserted — **combo-orthography**: a writing system's standardized production sequence as a game's combo system, the lexicon as the curated combo book, order rules as learnable combo grammar. S1 chartered as an independent Lab track; no fight protocol against R4/v3 (different literacy operations, nothing contested) | John + chat | LOCKED | — |

## 11. Playtest Checkpoint Log — append-only, newest on top (S1-D058)

The actual checkpoint messages handed to John, verbatim, kept alongside their
ship entries so the invitation and its eventual verdict both have a durable
record.

### Checkpoint — M2e-b1: the ink travels (2026-07-11, follows S1-D070; verdict pending — legibility/feel of the lock transition)

> **Where.** New artifact — note the **m2e**:
> ```
> cd ~/projects/inkblade && python3 -m http.server 8000
> ```
> `http://localhost:8000/inkblade-m2e.html?reset=1` (embedded core is enough
> to see everything; add `?pack=/packs/core.json,/packs/basic.json&reset=1`
> for the 500-tier. Corner stamp should read `S1-M2e-b1-20260711`).
>
> **What's new vs m2d:** the transition you asked for. When you complete a
> character, its object no longer silently pops in somewhere — now the
> character's strokes **gather into a gold drop of ink, the drop flies in an
> arc out into the scene, and the object grows out of the splash where it
> lands**. The pinyin + meaning now appear **next to the object** at the
> moment it lands (they used to flash center-screen at the writing box).
> Nothing about placement, burning, or timing changed — same dice, same
> clocks, the ink is just visible traveling between your writing and the
> world now.
>
> **What to try (~3 min):**
> 1. Write 木 — watch the strokes gather, the drop fly, and the tree grow
>    where it lands, named "mù / tree" beside it.
> 2. Write 林 — TWO drops launch, one after the other, and land as a grove.
> 3. Write 火 — the drop lands and becomes a burning campfire (it only
>    starts burning when it lands, never mid-air).
> 4. On the 500-tier URL, lock any character without a picture yet — the
>    drop becomes its stamp (same journey for every character).
> (`?char=木` etc. pins the rotation if it's slow to offer one.)
>
> **Verdict needed:** (a) is it now obvious WHICH character became WHAT
> object? (b) does the flight feel right for kids — the drop takes about a
> second; too slow/too fast? (c) does the name-next-to-the-object read
> better than the old center-screen banner? Nothing is hard-blocked on this
> — the motion-parallax camera (next cosmetic step) plans in parallel — but
> your feel verdict tunes the transition before it calcifies.

### Checkpoint — M2d-b1: every character earns a face, batch 1 (2026-07-11, follows S1-D067; VERDICT S1-D068: ACCEPTED — "batch1 symbols are acceptable but we can improve later when the swappables are ready"; bespoke-batch scaling stops in favor of the sprite pack block; core loop declared fixed, visuals cosmetic, sandbox interactions named the next MAJOR milestone)

> **Where.** New artifact — note the **m2d**:
> ```
> cd ~/projects/inkblade && python3 -m http.server 8000
> ```
> `http://localhost:8000/inkblade-m2d.html?pack=/packs/core.json,/packs/basic.json&reset=1`
> (use the 500-tier URL — most of the new faces live there; corner stamp
> should read `S1-M2d-b1-20260710`).
>
> **What's new:** 100 characters that used to be blank stamps now have real
> pictures, plus the duplicates you flagged are fixed. Highlights:
> - **大 is no longer a second mountain** — it's a big figure with arms
>   spread wide (the actual pictograph), and it wanders like a person.
> - New one-of-a-kind symbols: 马 a horse that ambles (and flees fire!),
>   门 a gate, 家 a little hut with a door, 心 a beating heart, 花 a flower,
>   风 rolling wind swirls, 电 a storm cloudlet with a flickering bolt,
>   车 a cart, 天 a glow in the sky.
> - Whole families: speech/thread characters (说话请谢红级…) plant little
>   fluttering banners; roof characters (安字实定客…) plant huts and
>   pavilions; sun characters (时明早晚春…) plant sky-glows; hand/walk
>   characters (打把找过还送…) plant working/strolling figures.
> - **The new things aren't just pictures** — the horse runs from fire, a
>   person will sit down to rest beside a hut, and a hut can catch fire and
>   grow back. Same reaction rules as everything else, via data tags.
>
> **What to try (~5 min):** write 大 then 山 (different now!), then 马, 家,
> 心, 花, 门 (`?char=马` etc. pins one if the rotation is slow to offer
> them). Then write 火 near the hut and watch what happens — and wait ~4
> minutes to see the hut come back.
>
> **Verdict needed (this gates scaling):** is this the right LOOK and
> feel for kids? Specifically: (a) do the new symbols read instantly to a
> child's eye, (b) is the brush style right or should they be bolder/more
> colorful, (c) any that miss (the horse is the one I'm least sure of)?
> 277 characters still lack faces — on your yes, I scale this approach
> (more radical families + more bespoke pictographs per batch, each batch
> checkpointed).

### Checkpoint — M2c-b2: the illustrated valley (2026-07-10, follows S1-D065; VERDICT 2026-07-11 (S1-D066 preamble): "yes good now" — hollow resolved, chunk C green-lit, symbols framed as fun-to-look + future-interaction prep)

> **Where.** Same artifact and URLs as last time:
> ```
> cd ~/projects/inkblade && python3 -m http.server 8000
> ```
> `http://localhost:8000/inkblade-m2c.html?reset=1` (add
> `&pack=/packs/core.json,/packs/basic.json` for the 500 tier).
> Reload the page if you still have the old build open — the canvas
> bottom-right should read `S1-M2c-b2-20260710`.
>
> **What's new vs. this morning's build (your "hollow" verdict):** the world
> now has a permanent backdrop — layered hills in the middle distance,
> clouds drifting across the sky (slow; one crossing takes a couple of
> minutes), and grass tufts + pebbles scattered across the ground, denser
> and bigger up close. You see it from the title screen, before writing
> anything. While you write it fades back with the rest of the world and
> blooms again on every lock — the fade is the game's focus choreography,
> not a bug.
>
> **What to try (~2 min):**
> 1. Open the URL and just look at the title screen — it should read as a
>    valley (hills, clouds, ground), not blank paper.
> 2. Play a few characters; watch the backdrop bloom back in color on each
>    lock, and check your planted trees/creatures now sit IN the landscape.
> 3. Glance back after a minute — a cloud should have visibly moved.
>
> **Verdict needed:** does the world still feel hollow, or does the 3D read
> land now? If it lands, chunk C (unique symbols for every character —
> designed for kids' eyes) starts immediately. If not, tell me what's
> missing from the picture and I'll extend the backdrop first. Also noted
> and logged for everything ahead: **this is a children's game** — chunk C's
> symbol designs will aim for picture-book clarity, and I ruled out an
> engine rewrite (no framework is in use; Phaser 3/4 are natively 2D-only
> anyway, and the real-3D route would mean rebuilding the game on three.js
> and discarding the entire verified test battery for no visual win a good
> backdrop can't deliver).

### Checkpoint — M2c-b1: the scroll gains depth + the legible ledger (2026-07-10, follows S1-D062; VERDICT S1-D063: depth reads, world hollow — backdrop needed; audience is children; no engine switch)

> **Where.** New artifact this time:
> ```
> cd ~/projects/inkblade && python3 -m http.server 8000
> ```
> `http://localhost:8000/inkblade-m2c.html?reset=1` — note the **m2c** (the
> old m2a is now a frozen snapshot and shows old behavior on purpose).
> For the 500-char tier: `http://localhost:8000/inkblade-m2c.html?pack=/packs/core.json,/packs/basic.json&reset=1`
>
> **What's new vs. m2a (your points 2 and 4):**
> 1. **Depth**: far things now shrink to well under a third of near size (was
>    ~half), the ground field visibly converges toward the center with
>    distance like a real receding floor, everything standing on the ground
>    casts a small contact shadow, distance dissolves continuously into mist,
>    and the paper itself now carries a sky/ground division. Compare a tree
>    planted high on the field vs one planted low — they should read as far
>    vs. near, not small vs. big.
> 2. **The character ledger** (bottom strip): no more 500 unreadable dots —
>    you now see a window of ~11 large tiles centered on the character you're
>    writing (gold ring), locked ones in cinnabar, plus a counter like
>    `12 / 509 · core+basic` telling you how much of the roster is yours.
>    Same layout at 23 chars and at 509.
>
> **What to try (~3 min):**
> 1. Open the 500-tier URL. The ledger should be instantly readable and the
>    counter should say `0 / 509 · core+basic`.
> 2. Lock 5–6 characters. Watch tiles turn cinnabar and the counter climb;
>    the window slides to follow whatever you're writing.
> 3. Write a few 木/林 until trees land both high (far) and low (near) on
>    the field — check the far/near size difference and the shadows.
> 4. Optional: `?char=木` pins the stage to trees if you want to fill the
>    field quickly.
>
> **Verdict needed:** (a) Does the world read as 3D now — or if not, what
> specifically still reads flat? (b) Is the ledger what you meant by fixing
> the character list, or did you want a different thing entirely (e.g. a
> full-screen browsable index)? Chunk C (giving all ~500 characters unique
> visual symbols) starts next regardless — but its **first batch** will stop
> for your quality verdict before I scale it.

### Checkpoint — M2a-b4: the aim mechanic retired (2026-07-07, follows S1-D060; closed without a fresh playtest — John's "start to implement the plan" (2026-07-10) directed Code onward to chunk B, and the b4 change was itself the execution of his explicit ruling)

> **Where.** Same artifact, same server:
> ```
> cd ~/projects/inkblade && python3 -m http.server 8000
> ```
> `http://localhost:8000/inkblade-m2a.html?reset=1`
>
> **What's new vs. what you just played:** Dragging past the end of your final
> stroke used to steer where the mark planted — that's gone. Placement is now
> a straight dice roll, same as everything else in the world. If a fire
> happens to land near a tree, it catches; if not, it doesn't. No more
> "aiming."
>
> **What to try:** Play a few rounds writing 木 (tree) and 火 (fire) back to
> back, without trying to control where anything lands. Watch for a fire and
> a tree happening to end up close together — that's the "collision" moment
> (it should still ignite exactly like before, just by accident now instead
> of on purpose).
>
> **Verdict needed:** Does this feel more like the "things collide and react"
> world you described, now that aiming is out of the loop?
>
> On your other three points — investigated, and confirmed all of them:
> 1. **3D**: there's no true 3D — it's a 2D canvas with a depth illusion
>    (things scale/fade by vertical position) from an earlier milestone. It
>    evidently isn't reading as "3D" to you. Folded into the next chunk
>    (canvas redesign) rather than treated as separate.
> 2. **Duplicate/missing symbols**: confirmed exactly — only 13 distinct
>    visual symbols exist in the whole game, and 489 of the 500 basic-tier
>    characters (98%) have no symbol at all, just a plain stamp with the
>    character printed on it. And yes, 大 (big) and 山 (mountain) literally
>    share one symbol, differing only by size — the duplicate spotted.
> 3. **Canvas redesign**: queued right after the 3D fix, since it needs room
>    for the expanded symbol set.
>
> **Plan, in order:** (B) canvas/UI redesign + fix the 3D legibility, then
> (C) a real pass at giving every character its own symbol — starting with a
> first batch (fixing 大/山, the numbers, and adding several new families)
> checkpointed on quality before scaling to the full 500 and beyond. Symbol
> design done in-house rather than outsourced — same procedural-drawing
> technique the good examples (fire/tree/mountain/sun/moon) already use, just
> needs to cover more ground. M2b (demand world) is on hold until these land.

---

## Appendix — Glossary

| Term | Meaning |
|---|---|
| **Combo-orthography** | This track's original contribution: standardized stroke production order as a combo system (§1.1) |
| **Dictation game** | The anti-pattern: writing as transcription to be graded |
| **Bucket** | One of the five v1 direction classes a slash resolves to (§5.1) |
| **Gold / ash** | Per-stroke ink feedback: in-sequence cut vs out-of-sequence cut |
| **Lock / fizzle** | Sequence matches the real lexicon vs doesn't; the stability rule (S1-D002) |
| **Combo book** | The player's collection of locked characters — the game's only collection system |
| **Slash layer / combo layer** | Motor gesture fun vs sequence-knowledge depth; Law 1 governs their balance |

*End of Charter v1.*
